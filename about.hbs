Third-party notices for the vilan toolchain binaries

The release binaries (vilan, vilan-lsp) statically link the Rust crates
listed below. Each license's text is reproduced in full, followed by the
crates it covers. Generated by cargo-about from Cargo.lock (see about.toml).

{{#each licenses}}
--------------------------------------------------------------------------------
{{{name}}}

Covers: {{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}

{{{text}}}
{{/each}}
