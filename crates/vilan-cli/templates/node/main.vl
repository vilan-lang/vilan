// The entry. `fun main` runs on start — there is no `main()` call at the
// bottom of the file. `print` needs no import — it is in the default
// prelude; everything outside the prelude is imported explicitly.

import pkg::greeting::greeting;

fun main() {
	print(greeting("world"));
}
