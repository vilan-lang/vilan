// Generated by `vilan bindgen` from `constructor_globals.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Widget` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Widget;

impl Widget {
	/// `new Widget(…)` — the host constructor, from `declare var Widget`.
	[extern(new, "Widget")]
	[platform("node")]
	external fun new(): Widget;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): str;

	[extern(method, "press")]
	[platform("node")]
	external fun press(self): void;
}

// `Widget` is a host constructor: its `new` and its statics are bound in
// `impl Widget` (proposal/bindgen.md §10.3).

/// `Reply` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Reply;

impl Reply {
	/// `new Reply(…)` — the host constructor, from `declare var Reply`.
	// TODO(bindgen): 1 additional overload(s) of `new` not represented — vilan
	// has one signature per name. Consider a differently-named binding per
	// overload:
	//   new(): Reply
	[extern(new, "Reply")]
	[platform("node")]
	external fun new(body: str): Reply;

	[extern(new, "Reply")]
	[platform("node")]
	external fun new_with_status(body: str, status: f64): Reply;

	[extern(method, "json")]
	[platform("node")]
	external fun json(self): str;

	[extern("Reply.json")]
	[platform("node")]
	external fun json_2(data: str): Reply;

	[extern("Reply.kind")]
	[platform("node")]
	external fun kind(): str;
}

// `Reply` is a host constructor: its `new` and its statics are bound in
// `impl Reply` (proposal/bindgen.md §10.3).

/// `Picture` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Picture;

impl Picture {
	/// `new Picture(…)` — the host constructor, from `declare var Picture`.
	[extern(new, "Picture")]
	[platform("node")]
	external fun new(): Picture;

	/// `new Frame(…)` — the host constructor, from `declare var Frame`.
	[extern(new, "Frame")]
	[platform("node")]
	external fun new_frame(): Picture;

	[extern(new, "Frame")]
	[platform("node")]
	external fun new_frame_with_width(width: f64): Picture;

	[extern(get, "src")]
	[platform("node")]
	external fun src(self): str;

	[extern(set, "src")]
	[platform("node")]
	external fun set_src(self, value: str): void;
}

// `Picture` is a host constructor: its `new` and its statics are bound in
// `impl Picture` (proposal/bindgen.md §10.3).

// `Frame` is a host constructor: its `new` and its statics are bound in
// `impl Picture` (proposal/bindgen.md §10.3).

/// `Stream` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Stream<R>;

impl Stream<type R> {
	/// `new Stream(…)` — the host constructor, from `declare var Stream`.
	[extern(new, "Stream")]
	[platform("node")]
	external fun new<R>(): Stream<R>;

	[extern(method, "read")]
	[platform("node")]
	external fun read(self): R;
}

// `Stream` is a host constructor: its `new` and its statics are bound in
// `impl Stream` (proposal/bindgen.md §10.3).

// TODO(bindgen): `Filters` is an object-typed global with no construct
// signature, so it is not a host class — there is no type for its members
// to hang off. Bind the ones you need as dotted globals:
// `[extern("Filters.member")] external fun member(…)`
//   declare var Filters: { readonly SHOW_ALL: number; }

// TODO(bindgen): `config` is an object-typed global with no construct
// signature, so it is not a host class — there is no type for its members
// to hang off. Bind the ones you need as dotted globals:
// `[extern("config.member")] external fun member(…)`
//   declare var config: { debug: boolean; }

/// `GadgetConstructor` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct GadgetConstructor;

impl GadgetConstructor {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): GadgetConstructor;

	// TODO(bindgen): construct signature `new(): Widget` — this describes a
	// constructor reached through a global value, which v1 cannot bind (see the
	// global-variable note)
}

// TODO(bindgen): `Gadget` is a constructor object typed by the named
// interface `GadgetConstructor`. bindgen recognizes the INLINE spelling
// (`declare var Gadget: { new(…): … }`) only. Move the construct signature
// inline, or write `[extern(new, "Gadget")]` by hand
//   declare var Gadget: GadgetConstructor
