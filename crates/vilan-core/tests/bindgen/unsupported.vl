// Generated by `vilan bindgen` from `unsupported.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

// TODO(bindgen): namespace `Legacy` is out of bindgen v1's scope
// (proposal/bindgen.md §5)
//   declare namespace Legacy { … }

// TODO(bindgen): module declaration `some-package` is out of bindgen v1's
// scope (proposal/bindgen.md §5)
//   declare module some-package { … }

// TODO(bindgen): ambient global augmentation is out of bindgen v1's scope
// (proposal/bindgen.md §5)
//   declare global { … }

// TODO(bindgen): TypeScript enum `Colour` is out of bindgen v1's scope
// (proposal/bindgen.md §5)
//   enum Colour { … }

// TODO(bindgen): `document` is a global VALUE; vilan's `[extern(…)]` forms
// bind a call or a receiver's property, never a bare global read. Bind its
// members as dotted globals instead: `[extern("document.member")] external
// fun member(…)`
//   declare const document: Document

// TODO(bindgen): `version` is a global VALUE; vilan's `[extern(…)]` forms
// bind a call or a receiver's property, never a bare global read. Bind its
// members as dotted globals instead: `[extern("version.member")] external
// fun member(…)`
//   declare var version: string

// `Conditional` is a transparent alias for `conditional type`; vilan has no type alias, so every
// use of it below is written out directly.

// `Mapped` is a transparent alias for `mapped type`; vilan has no type alias, so every
// use of it below is written out directly.

// `Keys` is a transparent alias for `keyof type`; vilan has no type alias, so every
// use of it below is written out directly.

// `Template` is a transparent alias for `template literal type`; vilan has no type alias, so every
// use of it below is written out directly.

/// `Awkward` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Awkward;

impl Awkward {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Awkward;

	// TODO(bindgen): TS `symbol` has no vilan equivalent
	[extern(get, "tag")]
	[platform("node")]
	external fun tag(self): any;

	[extern(set, "tag")]
	[platform("node")]
	external fun set_tag(self, value: any): void;

	// TODO(bindgen): TS intersection `{ … } & { … }` has no vilan equivalent
	// (no structural types) — widened to `any`
	[extern(get, "both")]
	[platform("node")]
	external fun both(self): any;

	[extern(set, "both")]
	[platform("node")]
	external fun set_both(self, value: any): void;

	// TODO(bindgen): the bare `Function` type says nothing about its signature
	[extern(get, "callback")]
	[platform("node")]
	external fun callback(self): any;

	[extern(set, "callback")]
	[platform("node")]
	external fun set_callback(self, value: any): void;

	// TODO(bindgen): `Partial<…>` is a mapped utility type, out of v1 scope
	// (§3.11)
	[extern(get, "partial")]
	[platform("node")]
	external fun partial(self): any;

	[extern(set, "partial")]
	[platform("node")]
	external fun set_partial(self, value: any): void;

	// TODO(bindgen): call signature `(value: string): void` — a callable object
	// has no vilan form (a value is not a function)

	// TODO(bindgen): computed member name `[Symbol.iterator]` is out of bindgen
	// v1's scope

	// TODO(bindgen): call signature `(): void` — a callable object has no vilan
	// form (a value is not a function)

	// TODO(bindgen): private member `private secret` is out of bindgen v1's
	// scope

	// TODO(bindgen): property `string` has no declared type
}
