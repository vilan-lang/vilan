// Generated by `vilan bindgen` from `functions.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

[extern("greet")]
[platform("node")]
external fun greet(name: str): str;

[extern("log")]
[platform("node")]
external fun log(message: str): void;

[extern("log")]
[platform("node")]
external fun log_with_level(message: str, level: str): void;

[extern("total")]
[platform("node")]
external fun total(values: List<f64>): f64;

[extern("load")]
[platform("node")]
async external fun load(url: str): str;

[extern("every")]
[platform("node")]
external fun every(values: List<str>, predicate: |str| bool): bool;

[extern("watch")]
[platform("node")]
external fun watch(handler: |f64| void): void;

[extern("commit")]
[platform("node")]
external fun commit(handler: async |f64| str): void;

// TODO(bindgen): rest parameter `...parts` — vilan has no variadic
// parameters, so the binding takes the whole `List` as one argument and the
// host receives it as a single array rather than spread across its own
// parameters
[extern("join")]
[platform("node")]
external fun join(parts: List<str>): str;

[extern("pair")]
[platform("node")]
external fun pair(): (str, f64);

[extern("nothing")]
[platform("node")]
external fun nothing(): void;

// TODO(bindgen): TS `never` — vilan's `Never` is internal-only and cannot
// be written; this function may not return normally
[extern("halt")]
[platform("node")]
external fun halt(): void;
