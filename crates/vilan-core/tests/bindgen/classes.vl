// Generated by `vilan bindgen` from `classes.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Widget` — a host class, bound as an opaque handle.
external struct Widget;

impl Widget {
	[extern(new, "Widget")]
	[platform("node")]
	external fun new(title: str): Widget;

	[extern(new, "Widget")]
	[platform("node")]
	external fun new_with_open(title: str, open: bool): Widget;

	[extern("Widget.create")]
	[platform("node")]
	external fun create(title: str): Widget;

	[extern("Widget.version")]
	[platform("node")]
	external fun version(): str;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): str;

	[extern(get, "label")]
	[platform("node")]
	external fun label(self): str;

	[extern(set, "label")]
	[platform("node")]
	external fun set_label(self, value: str): void;

	[extern(method, "render")]
	[platform("node")]
	external fun render(self): void;

	[extern(method, "resize")]
	[platform("node")]
	external fun resize(self, width: f64, height: f64): bool;

	[extern(method, "load")]
	[platform("node")]
	async external fun load(self, url: str): str;
}
