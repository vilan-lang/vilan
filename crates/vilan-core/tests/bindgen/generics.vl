// Generated by `vilan bindgen` from `generics.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Box` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Box<T>;

impl Box<type T> {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Box<T>;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): T;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: T): void;

	[extern(method, "map")]
	[platform("node")]
	external fun map(self, next: T): Box<T>;
}

/// `Pair` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Pair<K, V>;

impl Pair<type K, type V> {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Pair<K, V>;

	[extern(get, "key")]
	[platform("node")]
	external fun key(self): K;

	[extern(set, "key")]
	[platform("node")]
	external fun set_key(self, value: K): void;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): V;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: V): void;
}

/// `Defaulted` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Defaulted<T>;

impl Defaulted<type T> {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Defaulted<T>;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): T;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: T): void;
}

/// `Bounded` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Bounded<T>;

impl Bounded<type T> {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Bounded<T>;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): T;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: T): void;
}

[extern("identity")]
[platform("node")]
external fun identity<T>(value: T): T;
