// Generated by `vilan bindgen` from `interfaces.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

// Synthesized by bindgen for an anonymous TypeScript object type.
/// `OptionsOrigin` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct OptionsOrigin;

impl OptionsOrigin {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): OptionsOrigin;

	[extern(get, "x")]
	[platform("node")]
	external fun x(self): f64;

	[extern(set, "x")]
	[platform("node")]
	external fun set_x(self, value: f64): void;

	[extern(get, "y")]
	[platform("node")]
	external fun y(self): f64;

	[extern(set, "y")]
	[platform("node")]
	external fun set_y(self, value: f64): void;
}

/// `Options` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Options;

impl Options {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Options;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): str;

	[extern(get, "title")]
	[platform("node")]
	external fun title(self): str;

	[extern(set, "title")]
	[platform("node")]
	external fun set_title(self, value: str): void;

	/// Property `open` may be `null`/`undefined` at the host — the bare type is
	/// bound (see the header); guard at the call site.
	[extern(get, "open")]
	[platform("node")]
	external fun open(self): bool;

	[extern(set, "open")]
	[platform("node")]
	external fun set_open(self, value: bool): void;

	[extern(get, "tags")]
	[platform("node")]
	external fun tags(self): List<str>;

	[extern(set, "tags")]
	[platform("node")]
	external fun set_tags(self, value: List<str>): void;

	[extern(get, "origin")]
	[platform("node")]
	external fun origin(self): OptionsOrigin;

	[extern(set, "origin")]
	[platform("node")]
	external fun set_origin(self, value: OptionsOrigin): void;

	[extern(method, "apply")]
	[platform("node")]
	external fun apply(self, value: str): void;

	[extern(method, "measure")]
	[platform("node")]
	external fun measure(self): f64;

	[extern(get, "computed")]
	[platform("node")]
	external fun computed(self): str;

	[extern(set, "computed")]
	[platform("node")]
	external fun set_computed(self, value: str): void;
}
