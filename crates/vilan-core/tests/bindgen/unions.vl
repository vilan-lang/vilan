// Generated by `vilan bindgen` from `unions.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Align` — the closed string set `"start" | "end" | "center"`.
/// The host boundary still speaks the raw JS string, so every binding that
/// takes one pairs a private raw extern with a match-wrapper.
enum Align {
	Start,
	End,
	Center,
}

// `Shape` is a transparent alias for `{ … } | { … }`; vilan has no type alias, so every
// use of it below is written out directly.

/// `Chart` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Chart;

impl Chart {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Chart;

	// TODO(bindgen): property `align` is the closed string set `Align` — the
	// raw `str` is bound; convert to and from `Align` at the call site
	[extern(get, "align")]
	[platform("node")]
	external fun align(self): str;

	[extern(set, "align")]
	[platform("node")]
	external fun set_align(self, value: str): void;

	[extern(method, "setAlign")]
	[doc(hidden)]
	[platform("node")]
	external fun set_align_2_raw(self, align: str): void;

	/// `set_align_2` — `set_align_2_raw` with its closed string sets spoken as enums.
	fun set_align_2(self, align: Align): void {
		self.set_align_2_raw(match align {
			Align::Start => "start",
			Align::End => "end",
			Align::Center => "center",
		})
	}

	/// This value may be `null`/`undefined` at the host — the bare type is
	/// bound (see the header); guard at the call site.
	[extern(get, "caption")]
	[platform("node")]
	external fun caption(self): str;

	[extern(set, "caption")]
	[platform("node")]
	external fun set_caption(self, value: str): void;

	/// This value may be `null`/`undefined` at the host — the bare type is
	/// bound (see the header); guard at the call site.
	[extern(get, "subtitle")]
	[platform("node")]
	external fun subtitle(self): str;

	[extern(set, "subtitle")]
	[platform("node")]
	external fun set_subtitle(self, value: str): void;

	// TODO(bindgen): TS union `string | number` widened to `any` — narrow by
	// hand
	[extern(get, "either")]
	[platform("node")]
	external fun either(self): any;

	[extern(set, "either")]
	[platform("node")]
	external fun set_either(self, value: any): void;

	// TODO(bindgen): discriminated union on `kind` — a vilan `enum` lowers to
	// `[tag, …payload]` while this host value is a tagged OBJECT, so the two
	// representations do not meet at the boundary. Bind it as an opaque handle
	// and dispatch on `[extern(get, "kind")]` by hand
	[extern(get, "shape")]
	[platform("node")]
	external fun shape(self): any;

	[extern(set, "shape")]
	[platform("node")]
	external fun set_shape(self, value: any): void;

	[extern(method, "inline")]
	[platform("node")]
	external fun inline(self, align: str): void;

	[extern(method, "maybeAlign")]
	[platform("node")]
	external fun maybe_align(self): void;

	[extern(method, "maybeAlign")]
	[doc(hidden)]
	[platform("node")]
	external fun maybe_align_with_align_raw(self, align: str): void;

	/// `maybe_align_with_align` — `maybe_align_with_align_raw` with its closed string sets spoken as enums.
	fun maybe_align_with_align(self, align: Align): void {
		self.maybe_align_with_align_raw(match align {
			Align::Start => "start",
			Align::End => "end",
			Align::Center => "center",
		})
	}
}
