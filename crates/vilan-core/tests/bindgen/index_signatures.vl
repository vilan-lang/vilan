// Generated by `vilan bindgen` from `index_signatures.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Lookup` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Lookup;

impl Lookup {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Lookup;

	// TODO(bindgen): string index signature `[key: string]: number` — vilan has
	// no open keyed-object type at a host boundary: `Map<str, T>` is a vilan
	// struct over a `NativeMap` keyed by `key.hash()`, not a plain host object.
	// Bind the keys you need as `[extern(get, "key")]` accessors
}

/// `ArrayLike` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct ArrayLike;

impl ArrayLike {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): ArrayLike;

	// TODO(bindgen): numeric index signature `[index: number]: string` — this
	// is an array-LIKE shape, NOT a JS array: `List<T>` needs `Symbol.iterator`
	// (`for`-in, `map`, `filter`, `fold` all throw without it), and a real
	// array with holes hands `undefined` to a `T`-typed slot. Convert at the
	// boundary (`Array.from`) and bind the result as `List<T>`
}

/// `Mixed` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Mixed;

impl Mixed {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Mixed;

	[extern(get, "length")]
	[platform("node")]
	external fun length(self): f64;

	[extern(set, "length")]
	[platform("node")]
	external fun set_length(self, value: f64): void;

	// TODO(bindgen): numeric index signature `[index: number]: string` — this
	// is an array-LIKE shape, NOT a JS array: `List<T>` needs `Symbol.iterator`
	// (`for`-in, `map`, `filter`, `fold` all throw without it), and a real
	// array with holes hands `undefined` to a `T`-typed slot. Convert at the
	// boundary (`Array.from`) and bind the result as `List<T>`
}

/// `Records` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Records;

impl Records {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Records;

	// TODO(bindgen): `Record<K, V>` is an open keyed host object; vilan's `Map`
	// is a struct over a `NativeMap` keyed by `key.hash()`, not a host object.
	// Bind the keys you need as `[extern(get, "key")]` accessors
	[extern(get, "byName")]
	[platform("node")]
	external fun by_name(self): any;

	[extern(set, "byName")]
	[platform("node")]
	external fun set_by_name(self, value: any): void;
}
