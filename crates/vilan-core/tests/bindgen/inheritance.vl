// Generated by `vilan bindgen` from `inheritance.d.ts` for the `node` platform.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Node` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Node;

impl Node {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Node;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): str;

	[extern(set, "id")]
	[platform("node")]
	external fun set_id(self, value: str): void;

	[extern(method, "remove")]
	[platform("node")]
	external fun remove(self): void;
}

/// `Element` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Element;

impl Element {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Element;

	[extern(get, "tag")]
	[platform("node")]
	external fun tag(self): str;

	[extern(set, "tag")]
	[platform("node")]
	external fun set_tag(self, value: str): void;

	[extern(method, "click")]
	[platform("node")]
	external fun click(self): void;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): str;

	[extern(set, "id")]
	[platform("node")]
	external fun set_id(self, value: str): void;

	[extern(method, "remove")]
	[platform("node")]
	external fun remove(self): void;
}

/// `Button` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Button;

impl Button {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Button;

	[extern(get, "id")]
	[platform("node")]
	external fun id(self): f64;

	[extern(set, "id")]
	[platform("node")]
	external fun set_id(self, value: f64): void;

	[extern(method, "press")]
	[platform("node")]
	external fun press(self): void;

	[extern(get, "tag")]
	[platform("node")]
	external fun tag(self): str;

	[extern(set, "tag")]
	[platform("node")]
	external fun set_tag(self, value: str): void;

	[extern(method, "click")]
	[platform("node")]
	external fun click(self): void;

	[extern(method, "remove")]
	[platform("node")]
	external fun remove(self): void;
}

/// `Boxed` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Boxed<T>;

impl Boxed<type T> {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Boxed<T>;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): T;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: T): void;
}

/// `StringBox` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct StringBox;

impl StringBox {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): StringBox;

	[extern(method, "trim")]
	[platform("node")]
	external fun trim(self): void;

	[extern(get, "value")]
	[platform("node")]
	external fun value(self): str;

	[extern(set, "value")]
	[platform("node")]
	external fun set_value(self, value: str): void;
}

/// `Orphan` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
// TODO(bindgen): `extends SomethingElsewhere` — `SomethingElsewhere` is not
// declared in this file, so its members are not copied in (bindgen v1 does
// not resolve across files, §2)
external struct Orphan;

impl Orphan {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Orphan;

	[extern(get, "own")]
	[platform("node")]
	external fun own_(self): str;

	[extern(set, "own")]
	[platform("node")]
	external fun set_own(self, value: str): void;
}
