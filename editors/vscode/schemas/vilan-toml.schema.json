{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "vilan.toml",
  "description": "A Vilan package, library, or project (workspace) manifest.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "package": {
      "type": "object",
      "description": "A package: a buildable, importable unit.",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "How other packages import this one. A valid identifier (letters, digits, underscore; no leading digit).",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "description": {
          "type": "string",
          "description": "A short, free-text description of the package."
        },
        "root": {
          "type": "string",
          "description": "The package source root, relative to this manifest. `entry` and every `pkg::` module resolve against it. Default: `src`.",
          "default": "src"
        },
        "entry": {
          "type": "string",
          "description": "The `build`/`run` entry file, resolved against `root`. Default: `main.vl`. Replaced by `[entry.<name>]` sections.",
          "default": "main.vl"
        },
        "target": {
          "type": "string",
          "description": "The default build platform. `none` is a pure library (core std only) — type-checkable but not buildable without a host.",
          "enum": ["node", "deno", "bun", "browser", "none"],
          "default": "node"
        },
        "split": {
          "type": "boolean",
          "description": "Emit route chunks: an eager bundle plus one lazily imported file per route arm, fetched when a navigation reaches it. `browser` legs only. Replaced by `[entry.<name>] split` when entry sections are declared.",
          "default": false
        },
        "default-entry": {
          "type": "string",
          "description": "Which `[entry.<name>]` `vilan run` executes when several are runnable. `--entry` overrides it.",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "dependencies": {
          "$ref": "#/definitions/dependencies"
        }
      }
    },
    "library": {
      "type": "object",
      "description": "A library: an importable unit with a `lib.vl` surface, no entry, and per-platform layers instead of one target.",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "How dependents import this library. A valid identifier (letters, digits, underscore; no leading digit).",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "description": {
          "type": "string",
          "description": "A short, free-text description of the library."
        },
        "root": {
          "type": "string",
          "description": "The base (shared) source root, relative to this manifest. Default: `src`.",
          "default": "src"
        },
        "dependencies": {
          "$ref": "#/definitions/dependencies"
        },
        "layer": {
          "type": "object",
          "description": "Overlay layers, keyed by layer name (`process`, `browser`, …). A module in a layer shadows the base for the platforms that layer serves.",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "root": {
                "type": "string",
                "description": "The layer's source root, relative to this manifest. Default: `src/<layer name>`."
              },
              "platform": {
                "type": "array",
                "description": "The platforms this layer serves — at least one. A runtime, a runtime version, or a family like `@process`.",
                "items": {
                  "type": "string",
                  "enum": [
                    "@process",
                    "node",
                    "node:24",
                    "node:*",
                    "deno",
                    "deno:2",
                    "deno:*",
                    "bun",
                    "bun:1",
                    "bun:*",
                    "browser"
                  ]
                }
              }
            }
          }
        }
      }
    },
    "project": {
      "type": "object",
      "description": "A workspace root: member packages, plus dependencies they can inherit.",
      "additionalProperties": false,
      "properties": {
        "packages": {
          "type": "array",
          "description": "Paths to the member package directories — the build set of `vilan build .` at the root.",
          "items": { "type": "string" }
        },
        "default-entry": {
          "type": "string",
          "description": "Which member package `vilan run` executes when several are runnable. `--entry` overrides it.",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "dependencies": {
          "$ref": "#/definitions/dependencies"
        }
      }
    },
    "entry": {
      "type": "object",
      "description": "Build entries of a multi-entry package, keyed by name; each name labels its `dist/<name>.js` output.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string",
            "description": "The entry file, resolved against the package `root`. Default: `<name>.vl`."
          },
          "target": {
            "type": "string",
            "description": "The entry's build platform. Must be a host — an entry is something to run.",
            "enum": ["node", "deno", "bun", "browser", "none"],
            "default": "node"
          },
          "split": {
            "type": "boolean",
            "description": "Emit route chunks: an eager bundle plus one lazily imported file per route arm, fetched when a navigation reaches it. `browser` legs only.",
            "default": false
          }
        }
      }
    },
    "build": {
      "type": "object",
      "description": "Build settings: the `run` hooks, plus the code-generation options (a `preset` initializes every knob; individual keys then override it).",
      "additionalProperties": false,
      "properties": {
        "run": {
          "description": "Commands to run through the platform shell BEFORE each build, in this manifest's directory. One bare command, or several in a list, run in order. They run on EVERY build; a hook that should be skipped when nothing moved goes in `[[build.hook]]` instead. A failure fails the build.",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        },
        "hook": {
          "type": "array",
          "description": "Named build hooks, run after every `run` command in declaration order. A hook that declares `inputs` and/or `outputs` is SKIPPED while none of them has moved (content, never mtime); `vilan build --rerun-hooks` runs it anyway.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "run"],
            "properties": {
              "name": {
                "type": "string",
                "description": "The hook's name — what the build reports on and what its freshness stamp keys on. Required, and unique in this manifest."
              },
              "run": {
                "description": "The command line (or lines) this hook runs through the platform shell, with your own privileges and no prompt.",
                "oneOf": [
                  { "type": "string" },
                  { "type": "array", "items": { "type": "string" } }
                ]
              },
              "inputs": {
                "description": "The files or directories the hook READS, relative to this manifest. A declared input that is missing is recorded as missing, so its appearance re-runs the hook. Plain paths — there is no glob matching.",
                "oneOf": [
                  { "type": "string" },
                  { "type": "array", "items": { "type": "string" } }
                ]
              },
              "outputs": {
                "description": "The files or directories the hook WRITES. A missing or hand-edited output re-runs it. Plain paths — there is no glob matching.",
                "oneOf": [
                  { "type": "string" },
                  { "type": "array", "items": { "type": "string" } }
                ]
              }
            }
          }
        },
        "preset": {
          "type": "string",
          "description": "A named starting point. `debug` is readable (indented, source names); `release` is minified and obfuscated.",
          "enum": ["debug", "release"],
          "default": "debug"
        },
        "indent": {
          "type": "boolean",
          "description": "Lay the output across lines with indentation (vs one flat line)."
        },
        "spaces": {
          "type": "boolean",
          "description": "Pad tokens with spaces (`a + b`, `[ 1, 2 ]`) vs tight (`a+b`, `[1,2]`)."
        },
        "readable-names": {
          "type": "boolean",
          "description": "Name generated identifiers after their source (most debuggable)."
        },
        "debug-names": {
          "type": "boolean",
          "description": "When `readable-names` is off, annotate obfuscated names with their source (`a/*count*/`)."
        },
        "infer-const": {
          "type": "boolean",
          "description": "Fold `let` initializers the compiler can evaluate, without the `const` keyword (on under `release`, off under `debug`)."
        }
      }
    },
    "macro": {
      "type": "object",
      "description": "The compile-time interpreter's budget.",
      "additionalProperties": false,
      "properties": {
        "fuel": {
          "type": "integer",
          "description": "Interpreter steps per macro/const run (default 1000000)."
        },
        "depth": {
          "type": "integer",
          "description": "Nested expansion rounds before the fixpoint gives up (default 16)."
        }
      }
    }
  },
  "definitions": {
    "dependencies": {
      "type": "object",
      "description": "Package dependencies, keyed by the name they are imported under. A table gives a local `path`, a `git` repository pinned to a `tag` or `rev`, or `project = true` to take the workspace root's declaration.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "description": "A registry dependency version requirement (e.g. \"1.2\"). Registry dependencies are not resolved yet."
          },
          { "$ref": "#/definitions/dependency" }
        ]
      }
    },
    "dependency": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "description": "A local directory, relative to this manifest."
        },
        "git": {
          "type": "string",
          "description": "The repository URL of a git dependency. The checkout must be a `[library]` with its `vilan.toml` at the root."
        },
        "tag": {
          "type": "string",
          "description": "The tag a git dependency pins (exactly one of `tag` / `rev`). A branch cannot be pinned — it moves."
        },
        "rev": {
          "type": "string",
          "description": "The commit SHA a git dependency pins (exactly one of `tag` / `rev`)."
        },
        "branch": {
          "type": "string",
          "description": "Rejected: a branch moves, so it pins nothing. Use `tag` or `rev`."
        },
        "project": {
          "type": "boolean",
          "description": "`project = true` takes this dependency's whole declaration from the workspace root's `[project.dependencies]` (paths there are relative to the project root). Combines with no other key."
        },
        "build-hooks": {
          "type": "boolean",
          "description": "Grant THIS dependency permission to run its own build hooks with your privileges. Absent means no. Nothing runs a dependency's hooks yet — every one is refused, opted in or not — so this records the grant, in the diff, ahead of the mechanism."
        },
        "version": {
          "type": "string",
          "description": "A registry dependency's version requirement. Registry dependencies are not resolved yet."
        },
        "registry": {
          "type": "string",
          "description": "The registry to fetch from. Not resolved yet."
        }
      }
    }
  }
}
