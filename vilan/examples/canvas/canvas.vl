// Generated by `vilan bindgen` from `canvas.d.ts` for the `browser` platform.
//
// Filtered to `--only HTMLCanvasElement` and everything its signatures reach —
// the rest of the declaration file is deliberately absent.
//
// This file is ordinary vilan source, not a build artifact: review it, edit
// it, and commit it. Nothing regenerates it behind your back.
//
// `// TODO(bindgen)` marks a TypeScript construct the generator could not
// express in vilan. Nothing was dropped silently — every one is named.
//
// ONE rule is stated here rather than repeated everywhere: vilan has no
// `null`, and `Option<T>` cannot cross a host boundary — it is a tagged
// array (`Some(v)` is `[0, v]`, `None` is `[1]`) that a third-party host
// neither produces nor reads. So a TypeScript type admitting `null` or
// `undefined` binds as the BARE type, and the absence is yours to guard.
// Each such binding carries a `///` note saying so.

/// `Event` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct Event;

impl Event {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): Event;

	[extern(get, "type")]
	[platform("browser")]
	external fun type_(self): str;

	[extern(method, "preventDefault")]
	[platform("browser")]
	external fun prevent_default(self): void;
}

/// `MouseEvent` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct MouseEvent;

impl MouseEvent {
	/// A fresh empty host object (`{}`) to fill in with the setters below —
	/// the `RequestInit` precedent (`std/src/fetch.vl`).
	[extern("Object")]
	external fun new(): MouseEvent;

	[extern(get, "offsetX")]
	[platform("browser")]
	external fun offset_x(self): f64;

	[extern(get, "offsetY")]
	[platform("browser")]
	external fun offset_y(self): f64;

	[extern(get, "shiftKey")]
	[platform("browser")]
	external fun shift_key(self): bool;

	[extern(get, "type")]
	[platform("browser")]
	external fun type_(self): str;

	[extern(method, "preventDefault")]
	[platform("browser")]
	external fun prevent_default(self): void;
}

/// `HTMLCanvasElement` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct HTMLCanvasElement;

impl HTMLCanvasElement {
	/// `new HTMLCanvasElement(…)` — the host constructor, from `declare var HTMLCanvasElement`.
	[extern(new, "HTMLCanvasElement")]
	[platform("browser")]
	external fun new(): HTMLCanvasElement;

	[extern(get, "width")]
	[platform("browser")]
	external fun width(self): f64;

	[extern(set, "width")]
	[platform("browser")]
	external fun set_width(self, value: f64): void;

	[extern(get, "height")]
	[platform("browser")]
	external fun height(self): f64;

	[extern(set, "height")]
	[platform("browser")]
	external fun set_height(self, value: f64): void;

	[extern(method, "getContext")]
	[platform("browser")]
	external fun get_context(self, context_id: str): CanvasRenderingContext2D;

	[extern(method, "toDataURL")]
	[platform("browser")]
	external fun to_data_url(self): str;

	// TODO(bindgen): `toDataURL` has 2 optional parameters, so it has 3 call
	// arities; the shortest and the longest are bound below. Add a binding of
	// the same symbol for an arity in between if you need one
	[extern(method, "toDataURL")]
	[platform("browser")]
	external fun to_data_url_with_type__and_quality(self, type_: str, quality: f64): str;

	[extern(get, "id")]
	[platform("browser")]
	external fun id(self): str;

	[extern(get, "title")]
	[platform("browser")]
	external fun title(self): str;

	[extern(set, "title")]
	[platform("browser")]
	external fun set_title(self, value: str): void;

	/// This value may be `null`/`undefined` at the host — the bare type is
	/// bound (see the header); guard at the call site.
	[extern(get, "onclick")]
	[platform("browser")]
	external fun onclick(self): |MouseEvent| void;

	[extern(set, "onclick")]
	[platform("browser")]
	external fun set_onclick(self, value: |MouseEvent| void): void;

	[extern(method, "addEventListener")]
	[platform("browser")]
	external fun add_event_listener(self, type_: str, listener: |Event| void): void;

	[extern(method, "removeEventListener")]
	[platform("browser")]
	external fun remove_event_listener(self, type_: str, listener: |Event| void): void;
}

/// `HTMLImageElement` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct HTMLImageElement;

impl HTMLImageElement {
	/// `new HTMLImageElement(…)` — the host constructor, from `declare var HTMLImageElement`.
	[extern(new, "HTMLImageElement")]
	[platform("browser")]
	external fun new(): HTMLImageElement;

	/// `new Image(…)` — the host constructor, from `declare var Image`.
	[extern(new, "Image")]
	[platform("browser")]
	external fun new_image(): HTMLImageElement;

	// TODO(bindgen): `new` has 2 optional parameters, so it has 3 call arities;
	// the shortest and the longest are bound below. Add a binding of the same
	// symbol for an arity in between if you need one
	[extern(new, "Image")]
	[platform("browser")]
	external fun new_image_with_width_and_height(width: f64, height: f64): HTMLImageElement;

	[extern(get, "src")]
	[platform("browser")]
	external fun src(self): str;

	[extern(set, "src")]
	[platform("browser")]
	external fun set_src(self, value: str): void;

	[extern(get, "complete")]
	[platform("browser")]
	external fun complete(self): bool;

	[extern(get, "id")]
	[platform("browser")]
	external fun id(self): str;

	[extern(get, "title")]
	[platform("browser")]
	external fun title(self): str;

	[extern(set, "title")]
	[platform("browser")]
	external fun set_title(self, value: str): void;

	/// This value may be `null`/`undefined` at the host — the bare type is
	/// bound (see the header); guard at the call site.
	[extern(get, "onclick")]
	[platform("browser")]
	external fun onclick(self): |MouseEvent| void;

	[extern(set, "onclick")]
	[platform("browser")]
	external fun set_onclick(self, value: |MouseEvent| void): void;

	[extern(method, "addEventListener")]
	[platform("browser")]
	external fun add_event_listener(self, type_: str, listener: |Event| void): void;

	[extern(method, "removeEventListener")]
	[platform("browser")]
	external fun remove_event_listener(self, type_: str, listener: |Event| void): void;
}

/// `CanvasGradient` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct CanvasGradient;

impl CanvasGradient {
	/// `new CanvasGradient(…)` — the host constructor, from `declare var CanvasGradient`.
	[extern(new, "CanvasGradient")]
	[platform("browser")]
	external fun new(): CanvasGradient;

	[extern(method, "addColorStop")]
	[platform("browser")]
	external fun add_color_stop(self, offset: f64, color: str): void;
}

/// `CanvasRenderingContext2D` — a host object, bound opaquely: its fields are reached through
/// `[extern(get/set, …)]` accessors, never as vilan struct fields (a vilan
/// `struct` is a positional array at runtime and would read the wrong slots).
external struct CanvasRenderingContext2D;

impl CanvasRenderingContext2D {
	/// `new CanvasRenderingContext2D(…)` — the host constructor, from `declare var CanvasRenderingContext2D`.
	[extern(new, "CanvasRenderingContext2D")]
	[platform("browser")]
	external fun new(): CanvasRenderingContext2D;

	[extern(get, "canvas")]
	[platform("browser")]
	external fun canvas(self): HTMLCanvasElement;

	// TODO(bindgen): TS union `string | CanvasGradient` widened to `any` —
	// narrow by hand
	[extern(get, "fillStyle")]
	[platform("browser")]
	external fun fill_style(self): any;

	[extern(set, "fillStyle")]
	[platform("browser")]
	external fun set_fill_style(self, value: any): void;

	[extern(get, "strokeStyle")]
	[platform("browser")]
	external fun stroke_style(self): str;

	[extern(set, "strokeStyle")]
	[platform("browser")]
	external fun set_stroke_style(self, value: str): void;

	[extern(get, "lineWidth")]
	[platform("browser")]
	external fun line_width(self): f64;

	[extern(set, "lineWidth")]
	[platform("browser")]
	external fun set_line_width(self, value: f64): void;

	[extern(get, "font")]
	[platform("browser")]
	external fun font(self): str;

	[extern(set, "font")]
	[platform("browser")]
	external fun set_font(self, value: str): void;

	[extern(method, "clearRect")]
	[platform("browser")]
	external fun clear_rect(self, x: f64, y: f64, w: f64, h: f64): void;

	[extern(method, "fillRect")]
	[platform("browser")]
	external fun fill_rect(self, x: f64, y: f64, w: f64, h: f64): void;

	[extern(method, "strokeRect")]
	[platform("browser")]
	external fun stroke_rect(self, x: f64, y: f64, w: f64, h: f64): void;

	[extern(method, "beginPath")]
	[platform("browser")]
	external fun begin_path(self): void;

	[extern(method, "closePath")]
	[platform("browser")]
	external fun close_path(self): void;

	[extern(method, "arc")]
	[platform("browser")]
	external fun arc(self, x: f64, y: f64, radius: f64, start_angle: f64, end_angle: f64): void;

	[extern(method, "arc")]
	[platform("browser")]
	external fun arc_with_counterclockwise(self, x: f64, y: f64, radius: f64, start_angle: f64, end_angle: f64, counterclockwise: bool): void;

	[extern(method, "moveTo")]
	[platform("browser")]
	external fun move_to(self, x: f64, y: f64): void;

	[extern(method, "lineTo")]
	[platform("browser")]
	external fun line_to(self, x: f64, y: f64): void;

	[extern(method, "fill")]
	[platform("browser")]
	external fun fill(self): void;

	[extern(method, "stroke")]
	[platform("browser")]
	external fun stroke(self): void;

	[extern(method, "fillText")]
	[platform("browser")]
	external fun fill_text(self, text: str, x: f64, y: f64): void;

	[extern(method, "drawImage")]
	[platform("browser")]
	external fun draw_image(self, image: HTMLImageElement, dx: f64, dy: f64): void;

	[extern(method, "createLinearGradient")]
	[platform("browser")]
	external fun create_linear_gradient(self, x0: f64, y0: f64, x1: f64, y1: f64): CanvasGradient;
}

// `HTMLCanvasElement` is a host constructor: its `new` and its statics are bound in
// `impl HTMLCanvasElement` (proposal/bindgen.md §10.3).

// `HTMLImageElement` is a host constructor: its `new` and its statics are bound in
// `impl HTMLImageElement` (proposal/bindgen.md §10.3).

// `Image` is a host constructor: its `new` and its statics are bound in
// `impl HTMLImageElement` (proposal/bindgen.md §10.3).

// `CanvasGradient` is a host constructor: its `new` and its statics are bound in
// `impl CanvasGradient` (proposal/bindgen.md §10.3).

// `CanvasRenderingContext2D` is a host constructor: its `new` and its statics are bound in
// `impl CanvasRenderingContext2D` (proposal/bindgen.md §10.3).
