# Vilan Backlog — open items (distilled 2026-07-18)

Distilled from [`backlog.md`](backlog.md), which stays as the historical record (shipped
items, their full context, the lessons recorded alongside them). This file carries **open
work only**, simplified. Ids continue the old file's stable numbering: a carried id is the
same item; ids marked **NEW** are new or split-out work; retired numbers stay retired.
Sizes S/M/L/XL; unordered within a section. Per `CLAUDE.md`: each non-trivial item gets a
formal definition + pinned tests before implementation.

**Restructure, 2026-08-03**: this file used to carry both open work and full ship
records side by side — about 70% of its lines were shipped items whose arc had
long since closed, which is why "open work only" had stopped being true. Every
entry that was fully SHIPPED/CLOSED as of this date had its complete body moved
**verbatim** to [`backlog.md`](backlog.md) (a new dated section there), leaving a
one-line tombstone here (`N. **Title — SHIPPED <date>** (<commit>; record:
backlog.md / <proposal>.md)`) — nothing was deleted, only relocated. Entries with
a shipped core but a still-live open tail (A7, A8, A16, C1, ...) kept their body
here.

**Status convention.** Every remaining entry below starts with a `STATUS:` line,
immediately after its title, one of:
- `STATUS: OPEN` — no known blocker, free to pick up.
- `STATUS: OPEN (blocked: <what>)` — genuinely stuck on a named prerequisite.
- `STATUS: OPEN (proposal-first)` — needs a design doc before code, per
  `CLAUDE.md`'s "prove a feature before implementing it".
- `STATUS: DEFERRED (<why/trigger>)` — deliberately not being worked, with the
  condition that would revive it.

**Why:** the reconciliation that produced this restructure found that a title's
own SHIPPED/CLOSED marker is not reliably greppable — several multi-line titles
carried the marker on their *second* line, invisible to a naive one-line
`grep`, and several entries simply stayed open for days after their arc actually
shipped because nothing came back to mark them. A guaranteed, fixed-position
`STATUS:` line (always immediately after the title, never buried mid-paragraph)
is the fix: a one-line grep of any entry's opening now tells the truth, and
"forgot to close it" becomes visible the next time this file is swept rather
than staying silent indefinitely.

---

## A. Reactive core & UI (`std::reactive`, `std::ui`)

7. **SSR — render and replace — v1 SHIPPED 2026-07-23** (S1 + S2; `ssr.md` is
   STATUS: OPEN (blocked: kolt/walkthrough SSR factoring undesigned; S3 demand-gated on real usage)
   the record, its §6 calls all settled per recommendation) — S1 (`ssr.md`
   §S1): `vilan/std/src/process/ui.vl`, the full read-once surface, handlers
   accepted-and-discarded, `mount`/`mount_root` omitted per §6a; gated by 22
   pins + the `ssr-render.vl` corpus golden + `tests/ssr_differential.rs`
   (browser-stub tree ≡ rendered string, byte for byte) + a `std::dom`
   platform-gate pin. S2 (`386074a`): `mount` clears before appending, plus
   `examples/ssr` and `tests/ssr_fullstack.rs` asserting rendered content
   BEFORE any JS and that boot REPLACES (one live root, server nodes detached,
   a click reaching the new tree); `guide/ssr.md` same commit. **S3 (the Wire
   initial-state blob) stays UNBUILT by decision** — demand-gated per §6c, so
   the double-fetch stands in v1. Recorded residues: `style_var` is
   snapshot-pinned but sits outside the differential (the DOM stub no-ops
   `style.setProperty`); and the S2 amendment's real open scope — **kolt and
   the walkthrough cannot SSR under v1** (their views read the live rpc client
   at build time, handlers capture it, browser-layer imports), an applicability
   factoring question filed nowhere but `ssr.md`'s amendment. Resumability =
   A7b, `ssr.md` §7. *Housekeeping, reconciled 2026-08-03: the top status
   block correctly reads SHIPPED (the "implementation starts 2026-07-23"
   phrasing survives only inside its "Prior status" historical trail, which
   is intentionally preserved, not live status); §S2's own body did still
   call `examples/ssr` a 3-package workspace, stale since D7 converted it to
   one package, two entries — corrected in `ssr.md` with a dated note.*
   Original entry: (L, was L–XL; REFRAMED + proposal drafted
   2026-07-22) — the user's call: **hydration is
   rejected, not deferred**. v1 = a render-only process-layer `std::ui` twin
   (string-tree View, read-once `bind_*`, `render(view) -> str`, the layer
   system doing the per-leg resolution) + replace semantics (`mount` clears
   first — a no-op for every existing app) + the full-stack e2e; SEO and
   first paint delivered with no adoption/addressing/mismatch machinery.
   The double-fetch stands in v1 (S3 = the Wire initial-state blob, gated on
   demand). Resumability = A7b, recorded in `ssr.md` §7 — the replace path
   builds nothing hydration would have built and nothing resumability
   discards. Open calls in `ssr.md` §6.

8. **UI styling — the tail** (core SHIPPED 2026-07-10; NARROWED TWICE)
   STATUS: OPEN (blocked: A7/G2 — only the entangled pieces remain)
   — the 2026-08-04 slices closed everything unentangled: slice 1
   (bind_styled, dark×pseudo, the <link> scaffold, the property head) and
   slice 2 (Color::rgba + relative .alpha, the Gradient family
   linear+radial, border_none, edge longhands, Display inline variants —
   60 property methods; class names additions-only; five raw sites
   unwound byte-identically; record ui-styling.md §0bis.3). REMAINING
   here: critical CSS and liveness-tied dead-style elimination, riding
   A7/G2's liveness-tied emission — nothing else. Adjacent open find:
   A22 (same-family rule ordering).

9. **`vilan.toml [build] run` hooks — SHIPPED 2026-07-25** (f4c9dd6, v0.16.0 grind 5; record: backlog.md)

13. **Hot module replacement (HMR) — SHIPPED 2026-07-21, arc complete** (record: backlog.md / hmr.md)

14. **NEW — Reactive residuals** (S–M each; split from shipped A6/A12) —
    STATUS: OPEN (narrowed TWICE 2026-08-04 — only the batch async-join residual remains, plus two owner questions)
    - `batch` async-join drain affinity: `batch` deliberately kept its `sync` fence at
      the turn merge; joining an ambient turn from an awaiting body is unresolved.
    - ~~The optimistic-write → reconcile lifecycle~~ — SHIPPED 2026-08-04
      (`Optimistic::over(signal)`: Pending/Confirmed/Rejected beside the
      value, generation-guarded overlap, batch-coherent transitions; the
      demand sweep found free `optimistic` has ZERO callers — the real
      demand is the STATE half; bycatch fixed a shipped-Draft two-signal
      coherence skew. TWO OWNER QUESTIONS in optimistic-lifecycle.md §9:
      a paint-less action-state cell? should caller-less free
      `optimistic` survive? Record: optimistic-lifecycle.md).
    - ~~`Draft`: auto re-push + debounce~~ — SHIPPED 2026-08-04 (record:
      draft-reconnect.md; rides SocketTransport.on_reconnect ordering;
      at-least-once stated; Timer-backed trailing debounce). OWNER QUESTION
      recorded there §4: should `bind_draft` get a DEFAULT debounce window?
      (Changes wire behavior of every bound input; one-line mechanism.)

15. **`run` in a multi-node workspace — SHIPPED 2026-07-22 (flag-only v1), follow-up SHIPPED 2026-07-25** (f4c9dd6; record: backlog.md)

16. **Bundle splitting — ARC COMPLETE 2026-08-04, S1–S4 all shipped** (S1 --print-chunks; S2 emission + swap_split gate; S3 preload/error-hook/generation-guard/watch-sweep/split-cost warning; S4 consumer decisions — run ignores split by doctrine, SSR serves chunks.json, playground guarded at source; break-even remeasured ≈6KB fixed cost, so no in-tree example splits and --print-chunks' verdict is the take-up instrument; record: backlog.md / bundle-splitting.md)

17. **HTML canvas layer — DISSOLVED INTO GENERATED BINDINGS 2026-08-06, CLOSED** (the owner's 2026-08-04 condition resolved in autogen's favor twice over: the v1 probe bound 99.8% of lib.dom's members, and v2's constructor idiom + `--only` shipped `examples/canvas` — 345 generated lines, 11/11 declarations, a real board drawing fills/arcs/text/click-repaint through bindings nobody typed, ONE hand-written entry-point extern; `std::browser::canvas` will never be hand-maintained, and `std::browser::dom` stays curated per bindgen.md §7; the tester's original problem is solved "for good" — any missing host API is now a bindgen invocation; canvas.md's design stands as the record; record: backlog.md / bindgen.md §10–§11)

18. **signals of collections: the mutate-in-place gap — SHIPPED 2026-08-03** (design (a) `Signal::update` per owner call; closure parameters gained the full parameter grammar and the scalar `Shared::write` view was fixed at root on the way; record: backlog.md / signal-update.md)

19. **`Signal<resource>` slips R10 — SHIPPED 2026-08-04** (R10 now reads the type per use, descending instantiated generic fields; the diagnostic names the route; record: backlog.md / signal-update.md §6)

20. **`map`/`filter`/`sort_by` element aliasing — SHIPPED 2026-08-04** (all FOUR list-producing methods aliased — `reverse` too, the entry's "happens not to alias" was wrong; closed by the store rule with A20+B54 as one slice; record: backlog.md / element-clones.md)

21. **`View.style_var` leaks its subscription — SHIPPED 2026-08-04** (one line: `effect` like every bind_*; disposal pinned via the router swap precedent; the ssr.md stub residue proved FALSE — the stub has folded style into the attribute since 309e2bb; record: backlog.md / ui-styling.md §0bis)

23. **style value types, round 2 — SHIPPED 2026-08-04** (the headline REVERSED: the 36 background sites needed nothing — filed off raw counts hours after §0bis.3 shipped their answers; the real supply: Length::zero/css, line_height_length, background_image/_size + blank-value const checks; 60 of 107 surviving raw sites unblocked, incremental-conversion hazard checked at all 36; the WEBSITE CONVERSION SHIPPED 2026-08-06 — 60/60 unblocked sites, 23 byte-identical and 37 computed-identical verified by before/after dist diff, the three by-design raws stand, zero table premises wrong; record: backlog.md / ui-styling.md §0bis.5)

22. **same-family style rules resolve by stylesheet order — SHIPPED 2026-08-04** (emission tier via the `*.sX` marker + object-level family drops; ZERO classes renamed — the re-mint permission went unspent; fixed a LIVE website bug (dead margin-left:auto); family inventory grew inset/background/flex; raw participates by property name; one documented tie hole (two different-part longhands) with zero instances; record: backlog.md / ui-styling.md §0bis.4)

## B. Type system & the type solver

3. **Variadic-generics tail** (narrowed 2026-08-04 — spread parameters
   STATUS: OPEN (remainder: keyof + symbolic pack concatenation, flat-tuple elision, B4-linked dispatch — tuple-value spread SHIPPED 2026-08-04, §T: concatenation typing, positional `..`, abstract packs spread alone, the f(..tuple) circle closed)
   SHIPPED as B3a: `...items: T` is a tuple parameter with a call
   convention, pure desugar, 32 red-first pins; call-site spreading
   `f(..existing)` refused-with-steer because it desugars to tuple-value
   spread — designed in §Surface syntax, NEVER BUILT, and the natural
   next slice; record variadic-generics.md §S). REMAINING: `keyof`;
   tuple-value spread; eliding the flat-tuple construction copy;
   trait-typed-value dispatch (→ B4).

4. **Trait objects / dynamic dispatch** (L; own proposal when demanded) — a bare-trait-
   STATUS: OPEN (amendment 2026-08-06, from B72's arc: a bare-trait value into a bounded generic today dies on B55's internal-error guard — `let x: A = bag; use_it(x)` reports "please report this program" for a user mistake; B4's design owes that path a real diagnostic even if the answer stays "no"; evidence method-resolution.md §10. Note also std's own accepting direction — iterator.vl returns a bare trait — is load-bearing, so any tightening must carve it out.)
   typed value (`let x: Display = …`) is a clean error today; making it work by value
   needs a `(value, vtable)` representation. Nothing demands it yet. **B29 note
   (2026-07-20): declared asyncness becomes load-bearing here** — B29 permits an
   async impl of a sync trait declaration because dispatch is monomorphized (the
   caller always knows the concrete callee); a vtable call knows only the
   declaration, so B4 must add the asyncness-agreement check (or async-adapt
   through the vtable) as part of its design.

11. **`!` / `?.` tail** (narrowed 2026-08-04 — the bare-`?` trait path
    STATUS: OPEN (design-gated only — try-and-lift.md §12.1/§12.2)
    SHIPPED: user `Lift` containers lift in expression regions, per-split
    dispatch under synthetic binder ids, 7 red-first pins; record §10/§11.
    REMAINING, both genuinely undesigned: §12.1 closure `!` (three coupled
    decisions: arg-becomes-Result, RpcOutcome×Try collision, which closures
    may host a `!`) and §12.2 Signal/Promise Lift opt-ins (the fork is
    load-bearing: Signal::map SUBSCRIBES, so `signal?` would mint an
    unowned subscription per render — the A21 leak shape). Also recorded
    §11: B29 does NOT cover a wrong-shaped Lift impl (zero declared
    members — the duck-typed lookup is the only gate).

34. **a self-referential local initializer overflows the analyzer — SHIPPED 2026-07-28** (record: backlog.md / local-shadowing.md)

35. **`std::style` breakpoint rules emit in an order that lets narrow beat wide — SHIPPED 2026-07-28** (record: backlog.md)

36. **the LSP red-flags process-twin-only names in two-entry packages — SHIPPED 2026-07-28** (record: backlog.md)

37. **`std::ui` cannot build inline SVG — SHIPPED 2026-07-28** (record: backlog.md)

38. **salvage tail retention for semantic tokens — SHIPPED 2026-08-03** (record: backlog.md / lsp-snapshot-consistency.md)

39. **the LSP's request path is doing too much work — SHIPPED 2026-08-02, all three legs, B39 COMPLETE** (record: backlog.md)

51. **Requirement polymorphism: the owner fence follows instantiation chains — SHIPPED 2026-08-02** (record: backlog.md / requirement-polymorphism.md)

50. **NEW — argument-list layout: prototyped, MEASURED, and deliberately NOT
    STATUS: DEFERRED (prototyped and measured, not worth building for today's one file — revisit if a real file makes the shape hurt)
    built** (M design, S implementation; 2026-08-01, closing R5's long-open
    question) — the escalation is easy: when a statement is still over budget
    after the normal split, break the argument list one argument per line. It
    was built behind a flag and measured — 8 files, 85 lines, over-budget
    81 → 66, idempotent — and SEVEN of the eight files come out clean
    (`browser/ui.vl`, `compare.vl`, `json.vl`, `rpc_server.vl`, `ws.vl`,
    `todo/store.vl`, `walkthrough/views.vl`).

    The eighth is the blocker, and it is a rule gap rather than a bug.
    `bytes.vl`:

        out = out + digits.substring(byte / 16, byte / 16 + 1) + digits.substring(
            byte % 16,
            byte % 16 + 1,
        );

    The over-budget cause is the BINARY chain; the escalation fires on the last
    call it can reach and leaves a dangling `digits.substring(` after a line
    that is still long. So argument layout needs a rule for WHICH call's
    arguments to break when a statement holds several, and "the last one
    reachable" is demonstrably not it. The binary layout already has its
    equivalent (the left operand wins, `binary_operand_layout`); this needs the
    same kind of decision made deliberately.

    Not worth inventing that rule for 15 lines: what remains over budget is
    dominated by string literals a formatter must not touch. Revisit if a real
    file makes the shape hurt. The prototype is reconstructible from this entry
    — a `Split::Arguments` permission, armed at the statement level when
    `over_line_budget` still holds after the normal reprint, consumed by
    `print_call_arguments`.

49. **a composite holding a spanning element — SHIPPED 2026-08-01** (record: backlog.md / composite-spanning-split.md)

48. **a chain's `})` seam splits regardless of width — SHIPPED 2026-08-01** (record: backlog.md / chain-seam-split.md)

47. **five std files silently BAIL the formatter — SHIPPED 2026-08-01** (record: backlog.md)

46. **`fun` signatures were outside the width rule — SHIPPED 2026-08-01** (record: backlog.md / signature-layout.md)

45. **imports were outside the width rule — SHIPPED 2026-08-01** (record: backlog.md)

44. **a spanning rendering exempted its whole statement from the budget — SHIPPED 2026-08-01** (record: backlog.md)

43. **a statement's split does not descend into a call's argument — SHIPPED 2026-08-01** (record: backlog.md / argument-tail-descent.md)

42. **struct literals were outside the width rule — SHIPPED 2026-08-01** (record: backlog.md)

41. **mid-chain comments orphan below the statement; attach them into split chains — SHIPPED 2026-08-01** (record: backlog.md / split-comment-attachment.md)

40. **LSP request handlers are not panic-fenced — SHIPPED 2026-07-28** (record: backlog.md)

29. **Trait conformance is name-only; signatures unchecked — SHIPPED 2026-07-20** (deb4a1d; residues closed 2026-07-28/29; record: backlog.md)

30. **NEW — `lazy`: defer to first demand** (M; **RATIFIED 2026-07-21;
    STATUS: DEFERRED (ratified design, nothing needs it yet — take up when real demand appears; RIDER 2026-08-06 from top-level-await.md §8.4: when S2 lands, J3's await-shaped initializer check must see THROUGH a lazy initializer)
    DEFERRED 2026-07-22** — user call: nothing needs it yet; take it up when
    demand appears. All §6 calls settled in `lazy.md`, slices §7 ready) — one semantic in two positions: lazy
    *parameters* (`fun expect(self, lazy message: str)` — call-by-need arguments,
    memoized, transparent to the callee; the compiler-managed form of the
    `unwrap_or_else` callback idiom) and lazy *module bindings* (`lazy let db:
    Database = open(..)` — first-use init, everything else identical to the eager
    global: loan-only, write-frozen, never drops; THE answer to the lazy-resource
    question, which also found and fixed the module-overwrite leak). Surface under
    the closure rule (no new alias kind or epoch event); thunks are sync +
    context-free + data-only in v1 (each the conservative direction). Open calls in
    the proposal §6: poison-vs-retry, the `unwrap_or` retrofit, spelling.

31. **module-level closure binding tree-shaken but still called — FIXED 2026-07-22** (record: backlog.md)

32. **unknown value name types as `void` and cascades — FIXED 2026-07-22** (record: backlog.md)

33. **module-level binding emission order — SHIPPED 2026-07-25, arc complete** (f9dec2f, 3f82aa2; record: backlog.md / b33-emission-order.md)

52. **any entry global resolves through any std module path — FIXED 2026-08-03** (record: backlog.md)

53. **Pattern captures alias their source — SHIPPED 2026-08-03, COMPLETE** (`0835c7d` in v0.23.6 covered destructure + unguarded match legs; the same-day follow-up arc closed the adversarial review's four gaps — `is`/guarded legs copy, resources MOVE out of generic captures per R11 reaching through `Wrap<T>`, SHARE×MOVE stratified, non-place tail seams caught — and decided `mut [a, b]` stamps its elements everywhere; 14 pins, 10 red-first; record: backlog.md / capture-clones.md)

54. **place-into-construction sharing — SHIPPED 2026-08-04** (closed with A20 as one slice: rule 1's copy lands at the store — constructions, `own` arguments, returned by-value parameters; record: backlog.md / element-clones.md)

55. **a bounded-generic call through a re-dispatched callee emits an empty function body — SHIPPED 2026-08-04** (TWO root causes, not one: the for-loop protocol arm emitted by bare id clearing the substitution, and Self-return specialization was whole-type-equality; plus a never-silent guard — an emitted body-less callable is now a hard error; record: backlog.md / iterator-adapters.md P2 correction)

56. **`for v in self` over a generic lowers to a native `for...of` — SHIPPED 2026-08-04** (Trait and Generic subjects now resolve through the bound and re-dispatch per monomorphization; a generic with no `next` bound is a clean compile error, not a runtime throw; record: backlog.md / iterator-adapters.md P3 correction)

57. **method resolution is registration-order; no ambiguity diagnostic — SHIPPED 2026-08-06** (inherent-over-trait + dup-inherent hard error + trait-vs-trait ambiguity + `Trait::method` disambiguation with `Type::method` tightened to match; the corpus agreement check found ZERO resolution changes — the entire blast radius was gap-b's dead duplicate, renamed so its pin survives; "inherent" is a property of the MEMBER; 20 pins, 9 plants; the missing `reconcile_type(Trait, Struct)` arm it exposed → B72, duplicate statics → B74; record: backlog.md / method-resolution.md §9)

58. **a bound on a trait's own generic parameter does not reach its default bodies — SHIPPED 2026-08-04** (TWO root causes, neither at the probe's steer: empty-substitution default emission — caught by B55's never-silent guard — and return-type inference re-binding a shared trait parameter; the spec gained the bound-as-assumption dual rule; I3's P4/S6 caveats discharged; record: backlog.md / iterator-adapters.md P4 correction)

59. **a guard needing a hoisted statement emits a dangling reference — SHIPPED 2026-08-04** (match lowering picks chain vs statement-sequence per leg needs; guarded captures copy ahead of a guard that reads them; plain-guard goldens byte-identical; record: backlog.md / capture-clones.md §5)

60. **a self-by-value call is not a move to the affine checker — SHIPPED 2026-08-04** (premise inverted: bare `self` is a LOAN per R3; the real rule shipped = a body may only consume what it owns, plus `own self` on Option's consuming combinators; `if is_some { unwrap }` on a resource now R7-rejects — use `match`; record: backlog.md / affine-moves.md)

61. **`sync` is unenforced for a void-returning closure parameter — SHIPPED 2026-08-04** (the checks conflated the adaptation question with the contract question; `Signal::update`'s `sync` now bites; zero in-tree reliance; record: backlog.md / signal-update.md §6)

62. **a `match` arm capture of a resource payload is never destroyed — SHIPPED 2026-08-04** (the rule: a capture of a CONSUMED subject owns like a `let`, a capture of a LOANED subject enrolls nothing; the twin `let`-destructure hole fixed too; 23 pins, zero pre-existing golden bytes moved; record: backlog.md / affine-moves.md §7)

63. **Option's remaining combinators at resource instantiations — SHIPPED 2026-08-04** (the elision predicate split from the diagnostic helper soundly — write-set-consulting; per-combinator rulings: is_some_and/ok_or/unzip/inspect/eq/or_else WORK, or/xor/unwrap_or reject correctly under 'a generic body cannot destroy a T' with one-error diagnostics; §6's eq/unwrap_or claims corrected; spec §6.3 reconciled with R3; record: backlog.md / affine-moves.md §8)

64. **a closure returning a captured local still aliases — SHIPPED 2026-08-04** (reframed on FRAMES: the returning frame owns what it declared; two cases — the captured local AND an `own`-parameter capture handed out repeatedly; closure_declared_bindings reuses R9's walk; bycatch: a pre-existing R9 false positive on `is`-introduced bindings fixed; record: backlog.md / the arc commits)

65. **a capture of a LOANED subject can be consumed — SHIPPED 2026-08-04** (collect_loaned_pattern_captures, the capture twin of the B60 rule, riding MoveScan so R11's per-instantiation scan got it free; own diagnostic — 'copy the payload' proved an IMPOSSIBLE steer, there is no copy spelling for resources; record: backlog.md / affine-moves.md §9)

66. **a generic body's capture leaks at a resource instantiation — SHIPPED 2026-08-04, WIDENED** (the honest rule: everything in plan_scope's dropped set is a teardown a generic body cannot run — subsumes captures, let-locals, and the R2 overwrite position found and closed in the same arc; ALSO closed the compat find that map/is_some_and genuinely leaked — B63 §8.2's 'clean' claim was false; record: backlog.md / affine-moves.md §9)

67. **branch-divergent moves leak — SHIPPED 2026-08-04** (the filed diagnosis was half wrong: intersection is exact GIVEN R7; the defect was R7's reach cut by an over-broad R4 tail exemption, replaced by is-refinement — or_else/or proven legal in the design note first; diagnostic reuses ConditionalMove verbatim; record: backlog.md / affine-moves.md §9)

68. **`drop(f(x))` on a call result destroys nothing — SHIPPED 2026-08-04** (the filing was one case too narrow — the real split is WHERE THE TYPE COMES FROM; sink arguments now typed once post-solve, one shared recognizer across four consumers, never-silent guard; the generic drop-forwarding refusal now catches the untyped path too; record: backlog.md / affine-moves.md §9.5)

69. **`preset = "release"` miscompiles: colliding identifiers — SHIPPED 2026-08-04** (the TRUE blast radius was 57 of 105 corpus programs, not seven — G3's release-vs-release comparison masked defects in both halves; root cause: the re-allocation pass collected only id-keyed names, so every temp/monomorph name minted from the same alphabet could be reissued; the invariant now: every generated name is re-allocated or reserved, pools disjoint by construction; the gate's first run found a SECOND release-only bug — tight printing fused `3 - -2` into postfix `3--(2)`; playground never affected, wasm builds debug options; record: backlog.md / the release_emission pins)

70. **a tuple element that is a call or `if` loses its splice — SHIPPED 2026-08-06** (ELEVEN forms were broken, not two — calls of every kind, `if`/`else if`, blocks, `await`, `*view`, `const`, a bare parameter; the filed remedy "the cache's coverage" proved WRONG twice over — types are deliberately not interned, re-derived ids unhook every TypeId-keyed consumer; the fix: the tuple's own rule keeps per-element types, generics deliberately stay nested; the arity tail was SEVEN positions + the compare_type_rigid twin; 25 pins, the §T.8 ignore lifted, zero goldens moved; record: backlog.md / variadic-generics.md §T.8)

71. **a nested free `fun` is emitted twice — SHIPPED 2026-08-06** (the premise was incomplete — plain function bodies too, nothing to do with `impl`; the body walk no longer emits a declaration for being written, so an uncalled nested `fun` emits nothing, demand-driven like everything else; renamer twin-handling KEPT — monomorphized instances hit it 29× in the corpus; 5 pins; bycatch → B75; record: backlog.md)

72. **a trait-typed parameter rejects a concrete implementing value — SHIPPED 2026-08-06 as the steer** (outcome (ii), decisively: outcome (i) was BUILT far enough to measure — the accepted value reaches a bounded generic and dies on B55's never-silent guard, so acceptance is B4's question, not a missing arm; the spec had already refused twice; `fun show(v: Display)` now steers to `fun show<T: Display>(v: T)` with a note at the declaration, cross-module; unimplementing arguments keep the plain mismatch; 11 pins; record: backlog.md / method-resolution.md §10)

73. **NEW — a blanket trait impl beats a user's specific one by declaration order** (M;
    STATUS: OPEN (blocked: specificity design — written down 2026-08-06, method-resolution.md §9)
    found by B57's arc) — `impl type T with Into<T>` in `std/src/into.vl`
    matches every subject, so a user's own `impl Foo with Into<Bar>` is
    dedup'd to one home and loses to the blanket by declaration order.
    Unchanged from before B57 and explicitly out of its scope per
    method-resolution.md §3(b) — but now a written-down consequence
    rather than an accident. Needs a specificity rule (more-specific
    impl wins) or an overlap rejection.

74. **duplicate statics escape the duplicate-inherent check — SHIPPED 2026-08-06** (the sweep found ZERO live collisions — std's 115 statics, the corpus, examples, docs fences; the filed "different namespaces" guess was WRONG and is pinned: statics and methods share ONE namespace, and the mixed static/method pair was the worst case — resolved-then-arity-failed on an unreachable declaration, now a proper duplicate error; trait-homing guard proven load-bearing (removing it turns Duration::describe red); 10 pins; record: backlog.md)

75. **calling a fn-typed binding is rejected — SHIPPED 2026-08-06** (fn-coercion.md §4's "calling such a binding works as before" was a MIS-RECORD, corrected; the call resolver dispatched on what the subject WAS, never its type — it resolves through the declaration now, one path with direct calls; gated on B20's four exclusions (generic/async/methods/external), proven load-bearing: ungated, a generic binding MISCOMPILED (name minted from a disjoint pool) and async returned a bare Promise; refusals now name their rule; 13 pins, goldens unmoved; record: backlog.md / fn-coercion.md)

76. **NEW — backed enums: a language question, the owner's own** (M;
    STATUS: OPEN (proposal DRAFTED 2026-08-06 — backed-enums.md, 927 lines, awaiting owner review; §7.2 flagged for the owner's eye: may an external fun return a backed enum, given an exhaustive match compiles its last arm to a bare else; the survey's sharpest bycatch filed as B79)
    `enum Align { Start = "start" }` does not parse — the discriminant
    grammar is `= (-)? integer` (`parsing.rs:3310`). Evidence FOR: a
    C-like enum already lowers to its BARE discriminant (`Ordering::
    Greater` compiles to `1`, not a tagged array), so a string-backed
    enum would lower to its bare string — deleting bindgen's match-
    wrapper machinery and every hand-written std wrapper binding of the
    same shape. Pinned by `a_vilan_enum_cannot_carry_a_string_backing_
    value`, which goes red the day it lands. Questions at take-up: mixed
    backings, exhaustiveness over a backed set, `match` on raw values,
    and whether a backed enum still admits payloads.

77. **a user `impl List<type T>` makes cold compiles spuriously report against std's own List — FIXED 2026-08-06** (NOT solver ordering: the residual-generic leak check collapsed "which file declares this constraint" into HashMap<TypeId, SourceId> last-write-wins over randomly-seeded iteration — but `impl Subject<type T>` deliberately INHERITS the subject's constraint id, so one id honestly has several declaring files; the fix keeps the SET; 0/30 spurious over 30 cold attempts, plants 8/30 and 30/30 red; the fix stays inside the pre-existing file-granularity approximation (scope-precise noted as the stricter form); the hunt's survey filed as E38; record: backlog.md)

78. **the protocol loop drops a tuple element's type when the element IS the generic parameter — FIXED 2026-08-06** (NOT a tuple defect — a bare-parameter defect: the struct/enum subject arm read the element off `next`'s DECLARED return type without instantiating against the receiver, unlike the Trait arm beside it; struct fields, nested containers, Option methods, closure calls, `next_mut` over a generic container, enum subjects, and adapter chains were all red pre-fix — 8 pins, 6 red on revert; enumerate/zip were immune because their payloads are STRUCTURAL, which is why the whole I3 arc never saw it; bycatch filed as B80/B82; record: backlog.md)

79. **enum discriminants are unvalidated, and duplicates MISCOMPILE — SHIPPED 2026-08-06** (the family was BIGGER than filed — three more holes: `0xFF` silently 0 (a second route into the duplicate collision), `1_000` silently 1, and i64::MAX + an implicit successor PANICKED the debug compiler; the parser now carries the literal unreduced, hex is READ (the analyzer's range check always spoke radix 16), and the payload rule is the MIXED-enum rule per backed-enums.md §3.3; sweep found zero flips — only two explicit-discriminant enums exist, both legal; grammar.md's NUMBER drift and the undocumented is_numeric-conjunction representation rule fixed in the same arc; 13 pins; record: backlog.md / backed-enums.md §1.7)

80. **`for x in subject` over a concrete struct with no `next` is not diagnosed — SHIPPED 2026-08-06** (the native set is now a declaration-driven rule — external structs (host-shaped, no field array to walk), `Set`'s own lowering, arrays/tuples — via a new `external` flag (the fields-empty proxy would have exempted `struct Marker {}`, itself silently looping zero times); the enum twin had the same hole, fixed; WIDENED for the same root cause: a `next` declared returning non-`Option` drove the loop into zero iterations exit 0 — now an error; by-value `next(self)` deliberately kept (iterator-protocol.vl depends on it); 225-file sweep zero flips; `for x in map` refused, converging with B85's waiting pin; 9 pins; record: backlog.md)

81. **`is`-captures from a viewed subject alias it — SHIPPED 2026-08-06** (TWO seams, both B53's non-place family: accessor substitution assumed the subject temp is a stable snapshot — a view's write is an in-place Object.assign, so scalars (which "owe no copy") re-read the overwritten slot; and is_place_expr excluded Dereference so `*view` subjects collected NO capture candidates at all — B53's original defect in the one spelling its predicate couldn't see; doctrine per shape argued from the record: values materialize (a scalar read IS the copy), aggregates copy per B53, resources materialize BARE as loans with the place-path twin pinned to prove indistinguishability; the predicate is WRITABILITY, not view-ness — `&self` keeps the SHARE elision; 13 pins + 3 plants; bycatch → B88 (the place path's own seam) and B89; the enum-payload drop suspicion was probe idiom — verified dropping at the cut; record: backlog.md / capture-clones.md §6)

82. **a pattern match against a bare generic parameter checks vacuously — SHIPPED 2026-08-06 as a SPLIT** (the blanket error was built and REJECTED on evidence — it broke `View::swap(route, |current| match current {..})`, how every vilan app dispatches pages; the rule: a parameter declared by an ENCLOSING scope is abstract by construction and errors; one that merely arrived unsubstituted stays lenient; the unsoundness closed was real — a List "matched" an enum pattern, a str escaped a fun declared `: i32`, a trait default matched the wrong enum; the residual half is the closure-parameter instantiation gap → B90; 7 pins; record: backlog.md / spec types.md §5.7/§5.11)

83. **`Type::static()` never got B57's tiering — SHIPPED 2026-08-06** (inversion confirmed live — block order decided `Bag::default()`; B57's tiering now reaches statics through the shared rank_member_candidates; the trait tier STAYS reachable for statics (no alternative spelling exists), pinned; `Trait::static()` disambiguation does NOT exist and cannot on today's design — nothing to select an impl through — so the ambiguity diagnostic steers to the inherent declaration, the fix that works (the B65 lesson); sweep zero flips; DESIGNER RESIDUE for the owner: should a static be trait-reachable at all — method-resolution.md §12; 9 pins; record: backlog.md)

84. **two same-named members in ONE impl block are silently the second — SHIPPED 2026-08-06** (the root cause was the RECORD, not the check: Implementation::declarations was read back from a name-keyed scope map, so the second declaration overwrote the first before any check saw a pair; Scope now keeps declaration_order (repeats included) and the map is derived; WIDENED to one-block-one-name whatever trait homes it, covering trait bodies; sweep zero live duplicates — std-surface §1.1's pop-twice claim was STALE (declared once now, note corrected); bindgen's pin rewritten to assert the rejection it waited for; 10 pins; record: backlog.md / method-resolution.md §11)

85. **`for x in self` inside a container's own generic impl skips the native lowering — SHIPPED 2026-08-06** (the filing was wrong twice — not `self`, not generics: emission asked the ITERABLE EXPRESSION for a type it never had (expr_type_id is silent for parameters, call results, and derefs — plain `fun count(s: Set<i32>)` was equally broken); first attempt (widening expr_type_id) BROKE B70's unresolved tuple channel and was reverted; the root fix keeps the analyzer's already-inferred iterable type (Program::for_each_iterable_types), total by construction; eight broken shapes fixed and pinned, `let`/field always fine, List/str never broken (JS-native); set.vl's warning comment was documenting a fixed bug and is rewritten; the Map neighbour converged with B80's refusal; 7+ pins; record: backlog.md / std-surface.md §7.7.1)

86. **NEW — the top-level-await hole: await-shaped initializers are ungated, and Node misclassifies the output** (M;
    STATUS: OPEN (UNBLOCKED 2026-08-07 — §8.2 ratified; §8.1 ruled .mjs; in the v0.33.0 async-entry lane)
    found 2026-08-06 by J3's survey) — the diagnostic walks
    initializer_calls_of, so `await` on a Task-valued binding, a spawn
    handle, or a sync fun returning Task compiles clean and emits real
    TLA. Then the FIRST miscompile: the emitter parenthesizes await
    operands, `await (x)` is valid CommonJS, so Node's ESM detection
    never fires — an extern-free program dies at runtime with
    `ReferenceError: await is not defined` (externs mask it via their
    import lines). Fix per §5.2/§5.3: widen the check to await-shaped,
    fix the host classification. Zero corpus goldens contain the shape.

87. **NEW — the HMR adopt thunk cannot carry an await** (M;
    STATUS: OPEN (reachable only through B86's hole — moot if it closes, load-bearing if TLA is ever allowed)
    found 2026-08-06 by J3's survey) — a real watch round emits
    `return await (pending);` inside the `is_async: false` __hmr_adopt
    thunk: the rebuilt bundle DOES NOT PARSE (`node --check`:
    SyntaxError). The contract needs redesigning at three call sites
    plus the shim, not patching. Record: top-level-await.md §1.5/§4.

88. **NEW — an owned place's component write mutates under an `is`-capture** (M;
    STATUS: OPEN (found 2026-08-06 by B81's arc — the place path's own seam, one write-form over; report-don't-widen was right)
    `mut t = (7, 3); if t is (let a, let b) { t.1 = 99; print(b) }`
    prints 99, want 3 — a COMPONENT write mutates in place where whole
    assignment rebinds. B81's viewed rule is total for its class; this
    one needs alias analysis or unconditional materialization, which
    moves goldens corpus-wide and reopens B59's placement question.
    Record: capture-clones.md §6.5.

89. **NEW — reassigning a viewed enum to a shorter variant leaves stale payload slots** (S–M;
    STATUS: OPEN (found 2026-08-06 by B81's arc)
    `Object.assign(self, [1])` does not truncate: the old payload
    survives in the trailing slots — unreachable through the enum's own
    API but present, and for a RESOURCE payload, undropped. (The
    enum-payload drop itself was verified working at the cut — this is
    specifically the stale-slot residue after a shortening reassign.)

90. **NEW — a closure argument to a METHOD's generic escapes the pattern check** (M;
    STATUS: OPEN (found 2026-08-06 by B82's arc; #[ignore]d pin: a_closure_argument_to_a_methods_generic_gets_the_real_check)
    `Holder::apply<T>(self, value: T, render: |T| i32)` with an enum
    match inside the closure compiles against the wrong enum and runs —
    the FREE-FUNCTION twin substitutes and gets the full check today,
    which makes this a gap, not a design. The residual half of B82's
    leniency; the fix is the closure-parameter instantiation, a
    different subsystem than B82 touched.

91. **NEW — a `next` inherited from a trait default does not drive the loop** (S–M;
    STATUS: OPEN (found 2026-08-06 by B80's arc; #[ignore]d pin: a_next_inherited_from_a_trait_default_drives_the_loop)
    `impl Empty with Fixed<i32> {}` declares nothing, so
    method_member_candidates misses the inherited default and the loop
    used to fall through to the silent native form. B80 turns it into a
    clean error — the right interim state, not the right final one; the
    fix is Gap E's inheritance reaching the loop's member lookup.

92. **NEW — an unannotated `next` whose body yields nothing reaches the lowering** (S;
    STATUS: OPEN (found 2026-08-06 by B80's arc; #[ignore]d pin: an_unannotated_next_that_yields_nothing_is_diagnosed)
    throws TypeError at runtime — loud, so not B80's silent class, but
    it should be a compile error (the inferred return admits no Option).

93. **NEW — const_eval's dedup sorts on a partial key** (S;
    STATUS: OPEN (found 2026-08-06 by E38's audit; latent — no observed flip)
    const_eval.rs ~780/~864: the sort key is span.start alone while
    `dedup()` compares the whole tuple, so distinct tuples sharing a
    start can interleave and defeat the dedup. Same-family fix as E38's
    (widen the key); untouched by that arc to keep its diff shaped.

## C. Memory model

*Frame: `claims-and-epochs.md` (2026-07-18) — the one law behind all of it (a claim is
valid while its owner's epoch is unchanged; views = static proof, handles = dynamic
check) and the closure decision: **C4 is the last major change to the memory model** —
everything else here is implementation or refinement within the frame.*

4. **Deterministic destruction — SHIPPED 2026-07-19, Tier 1 complete S1–S5** (Tier 2 tracked under C1 `Weak<T>` / F4 native backend; record: backlog.md / destruction.md)

1. **`Weak<T>`** (M; **blocked on counting** — C4 shipped 2026-07-19, so the blocker is
   STATUS: OPEN (blocked: Tier 2 refcounting, the native arc)
   now precisely Tier 2's refcounting (the native arc); fully specified in
   `destruction.md` §10, incl. the scoped `get(&self): Option<&T> borrows self` twin
   from `claims-and-epochs.md` §5a) — deterministic
   `upgrade() → None` needs a release event, which only exists once handles are
   refcounted; GC-timing `WeakRef` was rejected (2026-07-07). No JS-side driver today:
   the host GC collects `Shared` cycles, and observe-deletion is `Arena` generations'
   job.

2. **Dynamic rule-4 remainder** (sized 2026-07-18 → **folded into F4's native arc**) —
   STATUS: OPEN (blocked: F4's native memory story)
   cross-handle aliased writes (two `Shared` handles, one cell) need runtime
   generations / poisoned views; semantically empty on JS (nothing dangles, and E3 of
   `view-invalidation.md` already fences the across-`await` window). Build it with the
   native memory story, likely debug-mode-only.

6. **Geometry-effect inference — `bumps`, the twin of `borrows` — SHIPPED 2026-07-19** (d595ed2, 9b5e3cf; record: backlog.md / rule4-completion.md)

7. **Wire-blessed handles — SHIPPED 2026-07-25** (f4c9dd6, v0.16.0 grind 5; record: backlog.md)

9. **NEW — `[service]` stores owning resources** (M–L; found 2026-07-19 by C4 S4a: the
   STATUS: DEFERRED (design when a real driver demands it)
   dispatcher collision) — a `[service]` struct with a resource field (`NotesStore { db:
   Database }`) is a resource by containment, and the generated dispatcher's per-`[rpc]`
   handler closures capture `self` — an R9 reject with no clean affine form (the
   handlers escape into the `Dispatcher`). v1's answer is the §9-blessed module-level
   idiom (walkthrough refactored to it; the collision is pinned as designed rejection).
   A service that truly OWNS its resources needs handler-ownership semantics — closures
   owning the store for the service's lifetime, which is Tier-2 counted-closure
   territory or an explicit service-lifetime owner object. Design when a real driver
   demands it; do not weaken R9 for it.

8. **`Arena.get` view-form migration — SHIPPED 2026-07-19** (record: backlog.md)

10. **`borrows`-returned views are invisible to rule 4 — SHIPPED 2026-07-19** (144d44e, 9b5e3cf; record: backlog.md / rule4-completion.md)

## D. Documentation

2. **Docs book Phase 3 — SHIPPED 2026-07-12** (bb98564; record: backlog.md)

3. **docs-harness fence extraction — SHIPPED 2026-07-22** (record: backlog.md)

4. **README refresh — SHIPPED 2026-07-22, tail swept 2026-07-29** (b1f42b8, 156166b; record: backlog.md)

5. **NEW — public traction plan** (M; user request 2026-07-22; a PLAN first,
   STATUS: OPEN (blocked: needs a dedicated session with the user)
   not execution; **prerequisite D10, the org move, SHIPPED — cleared**) —
   blogs, website, and other resources to gain public
   traction. Candidate skeleton: the book site is live (GitHub Pages) — a
   landing page in front of it; a "why vilan" essay + 2–3 technical deep
   dives with unusual substance (the claims/epochs law; the two-day
   handwritten-frontend arc with its differential method; HMR's in-heap
   transfer); show-don't-tell demos (the todo/kolt loop on video); F5/F7
   distribution as the on-ramp (marketplace + npm + brew); posting venues and
   cadence. NEEDS A DEDICATED SESSION with the user: public-exposure choices
   interact with the pseudonym discipline (what gets a face, what stays
   text), and voice/positioning are the user's calls to make, not defaults
   to assume.

6. **Docs approachability walk-through — CLOSED 2026-08-03** (record: backlog.md)

7. **`vilan/examples` cleanup; single-package becomes the default shape — SHIPPED 2026-07-25** (5c351a0; tails DONE 2026-08-03, E22 CLOSED 2026-07-29; record: backlog.md)

8. **`examples/walkthrough` README — SHIPPED 2026-07-25** (5c351a0; record: backlog.md)

10. **Move the repo to a proper org — SHIPPED** (5bb74b9, 0a0bdd4; AI_STANCE.md tail RESOLVED 2026-08-03 — owner call: personal-account link intended, stays; record: backlog.md / org-migration.md)

9. **"Vilan" casing sweep in docs and READMEs — DONE 2026-07-28** (record: backlog.md)

11. **Web playground — LIVE AND COMPLETE, NOTHING REMAINS BY CHOICE 2026-08-02** (S0–S4 all shipped, reconciled 2026-08-03; record: backlog.md / web-playground.md)

12. **De-AI-ism sweep of the docs — DONE 2026-07-28** (record: backlog.md)

## E. LSP & tooling

3. **Per-analysis leak + incremental analysis — SHIPPED, std-tax arc COMPLETE 2026-08-03** (record: backlog.md / analysis-reuse.md)

4. **Sub-file incremental parsing — CLOSED BY MEASUREMENT 2026-07-22** (record: backlog.md)

7. **Diagnostics audit — batch 7 — SHIPPED 2026-07-21** (record: backlog.md / diagnostics-ledger.md)

11. **Cross-source diagnostic notes — CLOSED as already-satisfied 2026-07-21** (record: backlog.md)

13. **formatter completion — SHIPPED 2026-07-22, zero-bail gate LIVE** (record: backlog.md)

12. **watch-round compile caching — SHIPPED 2026-07-21** (record: backlog.md)

14. **LSP construct snippets — SHIPPED 2026-07-23** (e29955e; record: backlog.md)

15. **pretty, colored CLI output — SHIPPED 2026-07-23** (9ae208b; record: backlog.md)

16. **cross-source diagnostic rendering — SHIPPED 2026-07-25** (v0.16.0 grind 4; residual CLOSED 2026-07-26, f4c9dd6; record: backlog.md)

17. **LSP module-attributed notes — SHIPPED 2026-07-25** (v0.16.0 grind 4; record: backlog.md)

20. **watch mode swallowed a save landing during the initial build — ROOT BUG FOUND AND FIXED 2026-07-25** (record: backlog.md)

19. **the `free_port` TOCTOU flake — SHIPPED 2026-07-26** (1442586, v0.16.0 grind 6; record: backlog.md)

18. **`vilan init` scaffold subcommand — SHIPPED 2026-07-25** (69ad118, v0.16.0 grind 2; record: backlog.md)

21. **test-suite speed audit — AUDIT DONE 2026-08-02** (record: backlog.md / suite-speed.md)

22. **seven of the nine examples have no build gate — SHIPPED 2026-07-29, COMPLETED 2026-08-03** (af49f87; record: backlog.md)

23. **a macro-DEFINING buffer re-leaks its whole world per length-changing edit — SHIPPED 2026-08-01** (record: backlog.md)

24. **`leak_tally` has no production surface — SHIPPED 2026-08-01** (record: backlog.md)

25. **run the suite's binaries in parallel — SHIPPED 2026-08-02** (record: backlog.md / suite-speed.md)

26. **batch inference's node runs — CLOSED NEGATIVE 2026-08-02** (record: backlog.md / suite-speed.md)

27. **parallelize the docs gate and interpreter cases — SHIPPED 2026-08-02** (record: backlog.md / suite-speed.md)

28. **share the LSP's analyzed test fixtures — CLOSED INTO THE STD-TAX ARC 2026-08-02** (record: backlog.md / analysis-reuse.md)

29. **cut the 16 s relink tax — CLOSED 2026-08-02, overtaken by events** (record: backlog.md / suite-speed.md)

30. **inference.rs is ~276 CPU-seconds of repeated std analysis — CLOSED INTO THE STD-TAX ARC 2026-08-02** (record: backlog.md / analysis-reuse.md)

31. **generate `external` bindings from TypeScript headers — v1 SHIPPED 2026-08-06** (`vilan bindgen <file.d.ts> --platform <p>`; ZERO dependencies — oxc failed the notices gate this entry demanded, a 1454-line declaration-only parser behind a swappable seam instead; FOUR ratified mapping rows corrected for one root cause — vilan aggregates have representations a host does not speak, and Option NEVER crosses a host boundary (per-arity bindings, std's own appendChild precedent); nothing dropped silently — TODO(bindgen) per construct; the lib.dom probe: 99.8% members / 65.8% declarations, 489k lines type-check clean, canvas binds — the A17 verdict; 60 pins; v2 → E37, backed enums → B76; record: backlog.md / bindgen.md §9–§10)

37. **bindgen v2 — (a)(b)(e) SHIPPED 2026-08-06** (the constructor idiom took lib.dom to 92.3% of declarations / 99.9% of members — §10.3's ~92% prediction exact; the residue is 186 declarations in three shapes, 183 of them GENUINE global values (`document`, `window`) which need a language-level "read a global" form — a language question, not a bindgen one; `--only <Type>` composes a complete closure and cut lib.dom 80%, but the DOM's element types are ONE strongly-connected component (~900 declarations via Node.ownerDocument and UIEvent.view), so a reviewable DOM slice needs the shallow/opaque mode recorded in §11.6 — for the third-party libraries bindgen targets, closures are small; the canvas example ships in examples/canvas on generated bindings, one hand-written entry-point extern; 26 pins; record: backlog.md / bindgen.md §11)
    STATUS: OPEN (remaining: (c) the oxc swap-in seam and (d) the override-table direction, both unscheduled; the 183-globals "read a global" language question; §11.6's shallow --only mode; §8 Q1/Q2/Q3/Q6 remain the owner's)

38. **diagnostics are hash-order nondeterministic, and the overlay truncates — SHIPPED 2026-08-06** (one canonical order at ONE seam — end of post_analysis_passes, key (source, span, msg, note), stable so every local answer-selecting sort stays load-bearing (all eleven kept, each with a stated reason); notes are a FIELD, not position-paired, so sorting cannot break pairing — but diagnostics/diagnostic_sources are PARALLEL VECTORS lazily padded, co-permuted at the seam, warnings too; THREE answer-flips fixed at source: the bound-note's file (least entity id), async violations' call site (least call id, all four sites), and a dedup that named `A` or `B` at random (constraint id joins the key — found by audit, not the survey); both lower-confidence sites CLOSED (id minting sorted — benign but invariant-keeping; drop-glue edge trail was NOT benign); 30/30 byte-identical harness with every fix planted red; residues → B93 (const_eval's latent key mismatch), E43 (the Set 12x cold cost), E42 (the wasm off-by-N, found by audit); goldens and both release gates unmoved; record: backlog.md)

39. **the hmr_swap e2e is wall-clock sensitive under contention — SHIPPED 2026-08-06** (the 20s literal wrapped round 1 — a FULL two-leg compile, measured 9.3s idle / 34s loaded, so the budget was 2x idle; WATCH_LIVENESS=300s where compiles are, round_budget = 4x measured round 1 elsewhere — E32's rule reached by calibration; an E20-vestige sleep replaced by an event wait; bycatch: http_get returned truncated bodies as complete — now "not yet"; stress-validated: hmr_swap green in BOTH concurrent full suites; the rest of the family's fixed clocks → E40, the vacuous negative windows → E41; record: backlog.md / suite-speed.md §6)

40. **the E32 treatment is unfinished across the e2e family — SHIPPED 2026-08-06** (the CI hotfix: both v0.32.0 CI failures were this family at their exact budgets on 4-core runners; the pass = one measured reference compile (a std::print-only build, 227ms idle) × 240, clamped — the multiplier is MEASURED, not felt: contention costs a flat ~2x across a 60x range of compile weight, so a 0.23s probe calibrates the machine; ELEVEN vilan-run watchdogs + three watch-round deadlines converted (rpc_http had SEVEN 60s sites, not five; assets.rs and watch_lifecycle.rs were unfiled siblings); cancellation's promptness pin kept a TIGHT window, marker-to-marker, plant-proven at 25.16s of a 32.1s run; node-boot-only clocks left standing by scope line, each surviving both stress suites; nested_nurseries re-confirmed as backlog drift (no clock at all); zero flakes in two concurrent full suites; record: backlog.md / suite-speed.md §7)

41. **NEW — hmr.rs's negative windows go vacuously green under load** (S;
    STATUS: OPEN (filed 2026-08-06 by E39's family sweep)
    `sse.assert_no(..., 2s)` and `!buffer_has(..., 700ms)` are
    load-sensitive in the WRONG direction — a slow machine makes them
    pass without testing anything. Plus two E20-vestige sleeps inside
    sequences E39 didn't otherwise touch. Wants event-anchored negative
    assertions (assert nothing arrives BETWEEN two positive events).

42. **NEW — the playground mis-attributes diagnostics when the entry has a parse error** (S–M;
    STATUS: OPEN (found 2026-08-06 by E38's audit; pre-existing, unrelated to ordering)
    vilan-wasm/src/lib.rs ~246-251 feeds the flat analyze_source index
    straight into program.diagnostic_source(index) without subtracting
    the parse-error prefix — the LSP (document.rs ~1062) and
    inference.rs both subtract it. Wrong file attribution by N wherever
    N parse errors precede. Wants its own pin in the wasm crate.

43. **NEW — importing `std::set` costs ~12x on cold analysis** (S–M;
    STATUS: OPEN (measured 2026-08-06 by E38's harness work — steered around, not investigated)
    ~6s per cold analysis vs ~0.5s for `std::print`. E40's pass found a
    SECOND cliff module: `std::time` alone costs 6s to import where
    `std::http`/`std::fetch`/`std::task` cost ~0.25s each (suite-speed.md
    §7) — two data points now, likely one mechanism. Unexplained;
    plausibly the Hashable bound graph or an import-closure cliff.
    Wants a measurement-first pass (VILAN_PHASE_TIMING per phase over
    the import set) before any fix.

32. **the cancellation test family is wall-clock-bounded — SHIPPED 2026-08-04** (compile split out of the timed window via compile()/run_js(); budgets unchanged, now measuring the program; plant-proven per test; stress-validated under two concurrent full suites, zero flakes; NOTE: only 3 of the 4 filed tests ever had the assertion — nested_nurseries_join_inside_out was backlog drift; record: backlog.md / suite-speed.md §5)

33. **the benchmarks e2e binds a fixed port — SHIPPED 2026-08-04** (FOUR fixed ports found, not one — 48231–48234 across throughput.vl and realtime.vl; all migrated to port 0 + read-back per E19; the literal collision reproduced pre-fix and survived post-fix under concurrent suites; record: backlog.md / suite-speed.md §5)

34. **the std::ui twin-surface parity gate — SHIPPED 2026-08-04** (analyzed-surface comparison, no regex; 14-then-18 allowlisted divergences each with its reason under the emitter-selected-vs-user-bindable rule; the twin INVENTORY itself is gated so std cannot grow an ungated twin; fired correctly on the A16 S3 surfaces the same day — its designed collision; record: backlog.md)

35. **one `CallGraph` per analysis — SHIPPED 2026-08-04** (the entry was wrong in both directions: SEVEN sites not three, init_order already shared; the ~9.5ms figure was an inferred intercept wrong ~17× — real cost 0.4-0.58ms/build, win ~1-2ms; context-threading's graph proven unshareable by a 32-red plant, spelled in the type; BYCATCH: VILAN_PHASE_TIMING=1 panicked every warm analysis inside catch_unwind — the instrument silently killed every warm analysis when enabled; fixed + pinned; record: backlog.md / const-eval.md §8.4)

36. **the release-preset differential gate — SHIPPED 2026-08-04** (release_differential.rs: 105 of 109 corpus programs run under both presets, 8-way chunked; marginal suite cost below measurement noise; plant-proven at 56 red; nursery.vl joins NOT_RUN with the E32 wall-clock reason; record: backlog.md)

## F. Backend & platform

3. **WASM backend** (L; far future) — emitter, backend-gated layers (`Layer.backend`),
   STATUS: DEFERRED (far future — no driver yet)
   and the memory-model lowering (linear allocator, C4 scope-end drops, ARC for
   `Shared` + C1 `Weak`) — the heavy part, shared with F4; do it once.

4. **Native backend** (XL; far future) — C-emit vs Cranelift vs LLVM; gate behind a
   STATUS: DEFERRED (far future — no driver yet)
   stable backend abstraction + a shared lowered IR, proven on F3 first. Absorbs C2's
   runtime check. A bundled tracing GC stays off the table (rev-1 goal #1).

5. **Project-model deferrals — SHIPPED 2026-07-25** (9c7567e, dcf32eb; record: backlog.md / distribution.md)

7. **Distribution Phase 2 — BUILT 2026-07-25, PROVISIONED 2026-07-29, all channels** (826a76f npm, c38c23e marketplace/Open VSX, 08ce573 homebrew; record: backlog.md / distribution.md)

8. **Windows support — ARC FULLY COMPLETE 2026-07-24** (f6171a7, v0.14.0; residuals tracked in windows-support.md §12; record: backlog.md / windows-support.md)

9. **repo → GitHub org — SHIPPED** (5bb74b9, 0a0bdd4; record: backlog.md, see also D10)

10. **Third-party notices for the distributed binaries — SHIPPED 2026-07-28** (record: backlog.md)

## G. Macros & const

2. **Const-eval tail** (S–M; recorded in `const-eval.md`) — **narrowed
   STATUS: OPEN (narrowed 2026-08-04; remainder is deferred-with-question, const-eval.md §8)
   2026-08-04**: the indirect-call gap was MIS-RECORDED (a silent runtime
   ReferenceError hole, not a refusal — closed: R-members refused as
   function values); const failures now carry a frame trace + the §4
   budget wording (which existed nowhere); the LSP's duplicate const pass
   deleted. REMAINING (each deferred-with-question in §8): expression-level
   const spans (needs per-node provenance or a spanned IR), cross-analysis
   memoization (cache-key question; measured 7-9% of warm analysis),
   a const budget knob ([macro] silently drops depth 512→16 — spec claim
   corrected). Liveness-tied emission stays A7-entangled.

3. **Inferred `const` — SHIPPED 2026-08-04** (the free-variable rule is the whole filter, decided by measurement — the span-sorted Expr::Local index that made it affordable also closed §8.4's free_locals cost; tighter budgets; BuildOptions.infer_const false under debug; effect-producing evaluations REFUSED for the invisible fold (the explicit form's output-swallowing is recorded in §9 for a future call); generic-body folding excluded after the differential caught a real wrong-fold; record: backlog.md / const-eval.md §9)

4. **The `macro_std` output contract** (M–L; renumbered — the old file carried two G2s;
   STATUS: DEFERRED (deferred by decision 2026-07-16 — revisit on demand)
   `proposal/macros-post-parse.md`, design complete, **deferred by decision**
   2026-07-16) — `Output` values over text `Source`; six open questions drafted; full
   normalization recorded as the §7 horizon.

## H. Parser & grammar

6. **Handwritten recursive-descent frontend — SHIPPED 2026-07-22, arc complete** (record: backlog.md / frontend.md)

7. **Interpolated triple-quoted strings — SHIPPED 2026-07-26** (1442586, v0.16.0 grind 6; record: backlog.md)

8. **Element syntax — ARC COMPLETE 2026-08-01, all five slices SHIPPED** (record: backlog.md / element-syntax.md)

9. **`mut` parameters — SHIPPED 2026-08-03** (open edge filed as B53/B54; record: backlog.md / mut-parameters.md)

## I. Collections

2. **Fixed-arrays tail** (M; `fixed-arrays.md` §7) — const-named / const-generic
   STATUS: OPEN (blocked: const-generics design — what `const N` means)
   lengths (`[u8; SIZE]`, `<const N>`): **blocked on design** — what `const N` means,
   the constraint form (`<const N: i32>`), and the staging fork (const-eval is
   post-analysis; lengths are needed mid-fixpoint) — proposal first. Then: `List` ↔
   `[T; n]` conversions (explicit, no coercion), slicing (wants a range type), generic
   `[T; N].len() → N`.

3. **iterator adapters + the pipeline ergonomics — ARC SHIPPED 2026-08-06** (M;
   STATUS: OPEN (arc SHIPPED 2026-08-06; §4 option (ii) REFUSED by owner ruling 2026-08-06 — the async-closure blocker and the measured ~5.5x are disqualifying, one meaning per name stands, the pin RETIRED 2026-08-06 with a section comment recording the ruling; remaining: S6/Iterable under B4)
   shipped: `Iterator<T>` on the repaired trait — adapters map/filter/
   take/skip/enumerate/zip/chain as trait defaults; terminations
   to_list/fold/for_each/count/any/all/rev, NO collect per the owner's
   ruling (the explicit family is primary, collect at most a future
   addition); to_set/to_map are List methods beside their bounds — a
   trait default cannot carry a bound its trait doesn't declare, an
   error at its own definition (§11, pinned); `List::iter` +
   `ListIterator`; `Range with Iterator<i32>`. One meaning per name:
   List does not implement Iterator, lazy spellings reachable only via
   `.iter()`. §4 option (ii) — eager forms over the adapters — was
   BUILT, MEASURED, REVERTED, and REFUSED (owner ruling 2026-08-06):
   an async closure cannot adapt through an adapter chain (adapt.vl
   fails re-expressed) and the lazy chain costs ~5.5x on a List source
   (two O(n) clones §8 said wouldn't exist). Bycatch: async dispatch
   candidates included statics (Promise::all collision) — fixed, 3
   pins; B77/B78 filed. Record: backlog.md / iterator-adapters.md §11)

4. **NEW — std surface audit: the missing basics** (S audit → M–L
   STATUS: OPEN (ARC COMPLETE 2026-08-06 — v1 2026-08-03 + the Map/Set tail 2026-08-06: entries/contains_value on Map, union/intersection/difference on Set; the brief's "ranked Map/Set/str rows" premise was WRONG — §3 has only an UNRANKED row (zero demand) and §1.3 closes str outright, the lane shipped the unambiguous members and refused the undesigned; Map/Set map/filter/for_each stay undesigned-by-choice (entries() gives the compositional route) — revisit on real demand; the eager enumerate/zip/take/skip question CLOSED AS DISSOLVED by owner ruling 2026-08-06; slicing → I2; bycatch filed as B85; record: backlog.md / std-surface.md §7.7)
   **Audit correction 2026-08-03**: List had FOURTEEN methods, not eleven —
   `get`/`first`/`last` already shipped, hiding in `option.vl`'s second
   `impl List` block (the same reachability mechanism behind the
   `to_string` bug). Original entry below.
   implementation; user request 2026-08-03) — the std API is thin in ways
   testers hit immediately. Verified examples: `List` has ELEVEN methods
   (new/push/pop/len/is_empty/map/filter/fold/for_each/sum/product) — no
   `reverse`, `sort`, `join`, `contains`, `index_of`, `first`/`last`,
   `insert`/`remove`, no slicing; `42.to_string()` EXISTS (`impl i32 with
   Display`, display.vl) but errors "i32 has no method 'to_string'"
   unless `std::display::Display` is imported — a DISCOVERABILITY failure
   masquerading as a missing feature; the steering diagnostic ("import
   std::display::Display to use it" — the trait-method-needs-import hint)
   should ride the diagnostics ledger. The audit slice: sweep
   List/Map/Set/str/number surfaces against what the examples, the
   walkthrough, and tester reports actually reached for; produce the
   ranked gap list with per-method semantics notes (sort stability,
   comparator form vs `Compare`, join's Display bound); then implement in
   dependency order. Constraint: eager `List` forms land now and do not
   wait on I3's adapter paper (I3 subsumes them lazily later). Related
   arrivals filed separately: canvas (A17), signal-of-collection
   mutation (A18), `mut` parameters (H9).

5. **the iterator protocol is unconformable as declared — FIXED 2026-08-06** (`next` takes `&mut self`; every conformer migrated; `Range` now carries the `with Iterator<i32>` clause it existed to sidestep; record: backlog.md / iterator-adapters.md §11)

## J. Concurrency

4. **NEW — Free-spawn lint** (S once unblocked; **blocked on Tier 2 counted closure
   STATUS: OPEN (blocked: Tier 2 counted closure environments)
   environments** — refined 2026-07-19 by C4 S4b) — the rule ("a spawn happens inside a
   `nursery` extent or an `OwnedNursery.enter` — anything else is a lint") cannot ship
   while std's three legitimate free spawns remain: `Draft.commit`, the RPC SSE pump,
   and streaming `on_open` are owned by capture-based cells or host-lifecycle
   connections, which R9 forbids from holding Tier-1 resources. They become ownable
   with §10's counted captures (or a connection-lifetime owner design); the lint ships
   the same day they migrate, with zero baked-in exceptions.

1. **Part C — the parallelism spine** (L–XL; `async-polymorphism.md` Part C) — workers
   STATUS: DEFERRED (far future — no driver application yet)
   over sendability (value semantics + `Wire` make a send a copy by construction);
   native fork-join later. Part B's scopes were designed not to foreclose it. No driver
   application yet.

3. **Top-level await — the design half** (M) — proposal DRAFTED 2026-08-06
   STATUS: OPEN (RATIFIED 2026-08-07 — the null recommendation is the owner's position, the hole closes (B86 unblocked); §8.1 ruled: .mjs for Node-leg build artifacts, against the draft's loosely-held marker recommendation)
   (top-level-await.md, 922 lines, eleven probes). THE PREMISE WAS WRONG:
   the shipped diagnostic is CALL-shaped, so an await-SHAPED initializer
   (`await` on a Task-valued binding, a spawn, a sync fun returning Task)
   compiles clean today and emits genuine ungated TLA — no corpus golden
   has ever contained the shape. Two live miscompiles hide behind the
   hole (B86, B87). The paper's recommendation: do NOT open TLA — close
   the hole; module-level spawn + async main + lazy cover the demand, and
   vilan's compile-time init-cycle rejection (a witness chain) beats
   ESM's silent deadlock. lazy is a COMPLEMENT (its ratified design rules
   async out); rider recorded on B30. main's discarded promise → J6.

5. **NEW — Async recorded opens** (S each; from Part B's ship record in
   STATUS: OPEN
   `async-polymorphism.md`) —
   - ~~`Task<Task<T>>` assimilation~~ — SHIPPED 2026-08-04 (assimilated at
     FORMATION, not await — fixes settle_all's handles too; the generic edge
     was the same bug; one `#[ignore]`d residual: declared-Task async returns
     need the async fixpoint at typing time; record: async-polymorphism.md).
   - Per-task cancel handles (**still deferred**; the race idiom composes from
     nursery-scoped cancel). *First field case, 2026-07-28 (the website's copy-button
     revert):* a click handler spawning `async { sleep(2400); revert() }` needs re-clicks
     to cancel the prior sleeper — `setTimeout`/`clearTimeout` semantics. Solved with a
     generation counter (`Shared<i32>`; stale sleepers wake and no-op), which is sound and
     race-immune, but the natural spelling would have been a cancel handle or a cancelable
     `std::time` timer. **Resolved 2026-07-28 by `std::time::Timer`** — the cancelable
     timer, not the cancel handle: "cancel a sleep" is what the field case (and most
     `clearTimeout` uses) actually wants, and a timer says it without giving every task a
     handle. `Timer::after(ms)`/`after_for` start on construction, `wait()` yields a
     memoized verdict (`true` fired / `false` cancelled), `cancel()` clears the host timer
     and settles `false`, idempotently. A cancelling nursery still only tears down the
     awaiting task — the timer's verdict is a separate thing. Per-task handles remain
     deferred: no field case has asked for cancelling a *computation*, only a delay.
   - The **free-spawn lint** (std's spawns audited + comment-marked; **blocked on C4's
     resource-owner story** — object-lifetime spawns need object-owned nurseries).
   - ~~The adapted-instance escape~~ — SHIPPED 2026-08-04 (A.4's factual half
     was FALSE — the escape produced `[object Promise]1` from a clean compile,
     incl. A.4's own compose example; per-instance field/return checks in the
     worklist's final pass, conservative-sound per A.4b's recorded residual;
     record: async-polymorphism.md A.4/A.4b).

6. **NEW — `main`'s promise is discarded** (S;
   STATUS: OPEN (filed 2026-08-06 from top-level-await.md §8.3)
   an `async fun main()`'s promise is dropped by the entry shim — a
   rejection surfaces only as an unhandled-rejection host event, not an
   exit code. Wants the shim to await and propagate (exit 1 on
   rejection), which is also what makes async main the honest
   substitute for TLA. Record: top-level-await.md §8.3.

---

**Recorded proposal tails** (trigger-based; tracked where they live, not re-listed
above): `numeric-types.md` §7 (f32 fround, sized parse family, numeric→`BigInt`, real
widths on native), `macro-engine.md` §11 (beyond-v1 engine tail), `hashable-keys.md`
(tuple keys, a real native hash table, identity keys), `validating-from-json.md` §7
(JSON-pointer error paths, range checks, reader dedup), `fn-coercion.md`
(generic/method coercion), and per-type `MIN`/`MAX` constants (want a static-member
story; revisit with F5/spec work).
