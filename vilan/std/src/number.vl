import pkg::compare::{ Eq, Ord, Ordering, PartialEq, PartialOrd };
import pkg::default::Default;
import pkg::math::PI;
import pkg::operators::{ Add, BitAnd, BitOr, BitXor, Div, Mul, Shl, Shr, Sub };
import pkg::option::Option::{ None, Some, self };

external struct u32;

impl u32 {
	[extern("Math.pow")]
	external fun pow(self, exponent: u32): u32;

	[extern("Math.min")]
	external fun min(self, other: u32): u32;

	[extern("Math.max")]
	external fun max(self, other: u32): u32;

	fun rem(self, m: u32): u32 {
		self - self / m * m
	}
}

impl u32 with Default {
	fun default(): u32 {
		0u32
	}
}

external struct i32;

impl i32 {
	fun diff(self, other: i32): i32 {
		if self > other {
			self - other
		} else {
			other - self
		}
	}

	/// Absolute value, exponentiation, and pairwise min/max — thin wrappers over the
	/// host `Math` functions (the receiver is passed as the first argument).
	[extern("Math.abs")]
	external fun abs(self): i32;

	[extern("Math.pow")]
	external fun pow(self, exponent: i32): i32;

	[extern("Math.min")]
	external fun min(self, other: i32): i32;

	[extern("Math.max")]
	external fun max(self, other: i32): i32;

	/// Truncated remainder — exact for every integer type (`/` truncates, §F2),
	/// the stopgap spelling until the `%` operator lands (backlog H5).
	fun rem(self, m: i32): i32 {
		self - self / m * m
	}
}

impl i32 with Default {
	fun default(): i32 {
		0i32
	}
}

external struct f64;

impl f64 {
	/// Floating-point math over the host `Math` functions.
	[extern("Math.abs")]
	external fun abs(self): f64;

	[extern("Math.sqrt")]
	external fun sqrt(self): f64;

	[extern("Math.pow")]
	external fun pow(self, exponent: f64): f64;

	[extern("Math.floor")]
	external fun floor(self): f64;

	[extern("Math.ceil")]
	external fun ceil(self): f64;

	[extern("Math.round")]
	external fun round(self): f64;

	[extern("Math.min")]
	external fun min(self, other: f64): f64;

	[extern("Math.max")]
	external fun max(self, other: f64): f64;

	// --- Trigonometry (radians) ---

	[extern("Math.sin")]
	external fun sin(self): f64;

	[extern("Math.cos")]
	external fun cos(self): f64;

	[extern("Math.tan")]
	external fun tan(self): f64;

	[extern("Math.asin")]
	external fun asin(self): f64;

	[extern("Math.acos")]
	external fun acos(self): f64;

	[extern("Math.atan")]
	external fun atan(self): f64;

	/// The two-argument arctangent: `y.atan2(x)` is the angle of the point
	/// `(x, y)` — the receiver is `y`, matching the host argument order.
	[extern("Math.atan2")]
	external fun atan2(self, x: f64): f64;

	// --- Exponentials and logarithms ---

	[extern("Math.exp")]
	external fun exp(self): f64;

	/// The natural logarithm (the host names it `log`; `ln` avoids the
	/// base-ambiguity trap).
	[extern("Math.log")]
	external fun ln(self): f64;

	[extern("Math.log2")]
	external fun log2(self): f64;

	[extern("Math.log10")]
	external fun log10(self): f64;

	[extern("Math.cbrt")]
	external fun cbrt(self): f64;

	/// `sqrt(x² + y²)` without intermediate overflow.
	[extern("Math.hypot")]
	external fun hypot(self, other: f64): f64;

	// --- Shape and structure ---

	/// `-1`, `0`, or `1` by sign (`-0` and `NaN` pass through, host semantics).
	[extern("Math.sign")]
	external fun sign(self): f64;

	/// The fractional part, truncation's complement: `x == x.trunc() + x.fract()`.
	fun fract(self): f64 {
		self - self.trunc()
	}

	/// Linear interpolation: `self` at `t == 0`, `to` at `t == 1`.
	fun lerp(self, to: f64, t: f64): f64 {
		self + (to - self) * t
	}

	fun to_radians(self): f64 {
		self * (PI / 180f)
	}

	fun to_degrees(self): f64 {
		self * (180f / PI)
	}

	/// Truncated remainder (sign follows the dividend), the method form of `%`.
	fun rem(self, m: f64): f64 {
		self % m
	}

	// --- Classification ---

	/// IEEE: NaN is the one value that is not equal to itself.
	fun is_nan(self): bool {
		self != self
	}

	[extern("Number.isFinite")]
	external fun is_finite(self): bool;

	fun is_infinite(self): bool {
		!self.is_finite() && !self.is_nan()
	}

	/// `self` confined to `min..=max`. The integers get this from `Ord`'s
	/// default; the floats are deliberately not `Ord` (NaN, see below), so they
	/// carry it themselves — the same recipe `Ord::clamp` uses.
	fun clamp(self, min: f64, max: f64): f64 {
		self.min(max).max(min)
	}
}

impl f64 with Default {
	fun default(): f64 {
		0f
	}
}

external struct BigInt;

impl BigInt with Default {
	fun default(): BigInt {
		0n
	}
}

// --- Ordering impls (backlog K2) — generated once, checked in (this file is
// world-loaded, so it must not dispatch macros). Native `<`/`>` bodies, like
// the `PartialEq` impls above: generic `T: Ord` code accepts the integer
// primitives; the floats are `PartialOrd` ONLY (the stated NaN answer).

impl i32 with Eq {}

impl i32 with PartialOrd {
	fun partial_compare(self, b: i32): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl i32 with Ord {
	fun compare(self, b: i32): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl u32 with Eq {}

impl u32 with PartialOrd {
	fun partial_compare(self, b: u32): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl u32 with Ord {
	fun compare(self, b: u32): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl i8 with Eq {}

impl i8 with PartialOrd {
	fun partial_compare(self, b: i8): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl i8 with Ord {
	fun compare(self, b: i8): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl u8 with Eq {}

impl u8 with PartialOrd {
	fun partial_compare(self, b: u8): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl u8 with Ord {
	fun compare(self, b: u8): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl i16 with Eq {}

impl i16 with PartialOrd {
	fun partial_compare(self, b: i16): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl i16 with Ord {
	fun compare(self, b: i16): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl u16 with Eq {}

impl u16 with PartialOrd {
	fun partial_compare(self, b: u16): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl u16 with Ord {
	fun compare(self, b: u16): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl i53 with Eq {}

impl i53 with PartialOrd {
	fun partial_compare(self, b: i53): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl i53 with Ord {
	fun compare(self, b: i53): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl u53 with Eq {}

impl u53 with PartialOrd {
	fun partial_compare(self, b: u53): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl u53 with Ord {
	fun compare(self, b: u53): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl BigInt with Eq {}

impl BigInt with PartialOrd {
	fun partial_compare(self, b: BigInt): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				Some(Ordering::Equal)
			}
		}
	}
}

impl BigInt with Ord {
	fun compare(self, b: BigInt): Ordering {
		if self < b {
			Ordering::Less
		} else {
			if self > b {
				Ordering::Greater
			} else {
				Ordering::Equal
			}
		}
	}
}

impl f64 with PartialOrd {
	/// NaN-honest: unordered comparisons yield `None`. The floats are
	/// deliberately NOT `Ord` — a total order over NaN would be a lie; use
	/// the `Math`-backed `min`/`max` METHODS for float pairs.
	fun partial_compare(self, b: f64): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				if self == b {
					Some(Ordering::Equal)
				} else {
					None
				}
			}
		}
	}
}

impl f32 with PartialOrd {
	/// NaN-honest: unordered comparisons yield `None`. The floats are
	/// deliberately NOT `Ord` — a total order over NaN would be a lie; use
	/// the `Math`-backed `min`/`max` METHODS for float pairs.
	fun partial_compare(self, b: f32): Option<Ordering> {
		if self < b {
			Some(Ordering::Less)
		} else {
			if self > b {
				Some(Ordering::Greater)
			} else {
				if self == b {
					Some(Ordering::Equal)
				} else {
					None
				}
			}
		}
	}
}

// --- Trait impls for the numeric primitives -----------------------------------
//
// Equality and the operator traits, so generic `T: PartialEq` / `T: Add` / ...
// code (and conditional impls over it) accepts the numeric primitives. Every
// body is the native operator: `+`/`==`/`&` on primitives always lower to
// native JS, so nothing recurses — a body runs only when reached through an
// explicit method call or a generic bound's dispatch; written `a + b` stays
// native and never touches these.

impl i32 with PartialEq {
	fun eq(self, b: i32): bool {
		self == b
	}
}

impl u32 with PartialEq {
	fun eq(self, b: u32): bool {
		self == b
	}
}

impl f64 with PartialEq {
	fun eq(self, b: f64): bool {
		self == b
	}
}

impl BigInt with PartialEq {
	fun eq(self, b: BigInt): bool {
		self == b
	}
}

impl i32 with Add {
	fun add(self, b: i32): i32 {
		self + b
	}
}

impl i32 with Sub {
	fun sub(self, b: i32): i32 {
		self - b
	}
}

impl i32 with Mul {
	fun mul(self, b: i32): i32 {
		self * b
	}
}

impl i32 with Div {
	fun div(self, b: i32): i32 {
		self / b
	}
}

impl u32 with Add {
	fun add(self, b: u32): u32 {
		self + b
	}
}

impl u32 with Sub {
	fun sub(self, b: u32): u32 {
		self - b
	}
}

impl u32 with Mul {
	fun mul(self, b: u32): u32 {
		self * b
	}
}

impl u32 with Div {
	fun div(self, b: u32): u32 {
		self / b
	}
}

impl f64 with Add {
	fun add(self, b: f64): f64 {
		self + b
	}
}

impl f64 with Sub {
	fun sub(self, b: f64): f64 {
		self - b
	}
}

impl f64 with Mul {
	fun mul(self, b: f64): f64 {
		self * b
	}
}

impl f64 with Div {
	fun div(self, b: f64): f64 {
		self / b
	}
}

impl BigInt with Add {
	fun add(self, b: BigInt): BigInt {
		self + b
	}
}

impl BigInt with Sub {
	fun sub(self, b: BigInt): BigInt {
		self - b
	}
}

impl BigInt with Mul {
	fun mul(self, b: BigInt): BigInt {
		self * b
	}
}

impl BigInt with Div {
	fun div(self, b: BigInt): BigInt {
		self / b
	}
}

// The bitwise/shift traits, for the two types the operators are defined on
// (the hex/bitwise slice): `i32` and `u32`. `f64` has no bitwise story and
// `BigInt`'s is unverified — deliberately omitted.

impl i32 with Shl {
	fun shl(self, b: i32): i32 {
		self << b
	}
}

impl i32 with Shr {
	fun shr(self, b: i32): i32 {
		self >> b
	}
}

impl i32 with BitAnd {
	fun bit_and(self, b: i32): i32 {
		self & b
	}
}

impl i32 with BitOr {
	fun bit_or(self, b: i32): i32 {
		self | b
	}
}

impl i32 with BitXor {
	fun bit_xor(self, b: i32): i32 {
		self ^ b
	}
}

impl u32 with Shl {
	fun shl(self, b: u32): u32 {
		self << b
	}
}

impl u32 with Shr {
	fun shr(self, b: u32): u32 {
		self >> b
	}
}

impl u32 with BitAnd {
	fun bit_and(self, b: u32): u32 {
		self & b
	}
}

impl u32 with BitOr {
	fun bit_or(self, b: u32): u32 {
		self | b
	}
}

impl u32 with BitXor {
	fun bit_xor(self, b: u32): u32 {
		self ^ b
	}
}

// --- Parity --------------------------------------------------------------------
//
// Concrete on the integer types: a generic form over `BitAnd` needs a way to
// conjure `one`/`zero` AT the generic type (a `One`/`Zero` seam vilan doesn't
// have), and parity on `f64` is undefined off the integers — so two honest
// one-liners per type beat a new numeric-tower trait.

impl i32 {
	fun is_even(self): bool {
		(self & 1) == 0
	}

	fun is_odd(self): bool {
		(self & 1) == 1
	}
}

impl u32 {
	fun is_even(self): bool {
		(self & 1u32) == 0u32
	}

	fun is_odd(self): bool {
		(self & 1u32) == 1u32
	}
}

// --- The sized numeric family (proposal/numeric-types.md) ---------------------
//
// Nominal widths on the JS backend: every sized integer lowers to a plain JS
// number (i53/u53 exact within ±2^53 — the measured f64-over-BigInt decision,
// §1), `f32` lowers to `f64`. Distinct types, range-checked literals, native
// operators, truncating integer division; width semantics become real on a
// non-JS backend.
//
// GENERATED ONCE by a macro and checked in as plain source: `number.vl` is
// loaded inside every macro world (via std's lib surface), and world files
// must not DISPATCH macros — a world expands with an empty macro scope
// (macro-engine.md §10). The family's shape is pinned by the numeric corpus
// program; regenerate by hand if the surface changes.

external struct i8;

external struct u8;

external struct i16;

external struct u16;

external struct i53;

external struct u53;

external struct f32;

// Conversions (`as_*`, numeric-types.md §4): truncate toward zero, then fold
// into the target's range (two's-complement wrap) — Rust-`as` semantics. All
// arithmetic happens in f64 space: `as_f64` widens (the host `Number` is the
// identity on numbers), the folds below shape the value, and a `label_*`
// extern re-types the result (identity at the JS level).

[extern("Number")]
external fun label_i8(value: f64): i8;

[extern("Number")]
external fun label_u8(value: f64): u8;

[extern("Number")]
external fun label_i16(value: f64): i16;

[extern("Number")]
external fun label_u16(value: f64): u16;

[extern("Number")]
external fun label_i32(value: f64): i32;

[extern("Number")]
external fun label_u32(value: f64): u32;

[extern("Number")]
external fun label_i64(value: f64): i53;

[extern("Number")]
external fun label_u64(value: f64): u53;

[extern("Number")]
external fun label_f32(value: f64): f32;

[extern("Number")]
external fun label_f64(value: f64): f64;

/// `value` truncated toward zero, folded into `0 ..= modulus - 1`.
fun fold_unsigned(value: f64, modulus: f64): f64 {
	let truncated = value.trunc();
	let wrapped = truncated % modulus;
	if wrapped < 0f {
		wrapped + modulus
	} else {
		wrapped
	}
}

/// The signed fold: two's-complement at `modulus`, so `half ..` goes negative.
fun fold_signed(value: f64, modulus: f64, half: f64): f64 {
	let wrapped = fold_unsigned(value, modulus);
	if wrapped >= half {
		wrapped - modulus
	} else {
		wrapped
	}
}

impl f64 {
	/// Truncation toward zero — the integer-division primitive (§2) and the
	/// first step of every narrowing conversion.
	[extern("Math.trunc")]
	external fun trunc(self): f64;
}

impl i8 {
	[extern("Math.abs")]
	external fun abs(self): i8;

	[extern("Math.pow")]
	external fun pow(self, exponent: i8): i8;

	[extern("Math.min")]
	external fun min(self, other: i8): i8;

	[extern("Math.max")]
	external fun max(self, other: i8): i8;

	fun rem(self, m: i8): i8 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		self
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl u8 {
	[extern("Math.pow")]
	external fun pow(self, exponent: u8): u8;

	[extern("Math.min")]
	external fun min(self, other: u8): u8;

	[extern("Math.max")]
	external fun max(self, other: u8): u8;

	fun rem(self, m: u8): u8 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		self
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl i16 {
	[extern("Math.abs")]
	external fun abs(self): i16;

	[extern("Math.pow")]
	external fun pow(self, exponent: i16): i16;

	[extern("Math.min")]
	external fun min(self, other: i16): i16;

	[extern("Math.max")]
	external fun max(self, other: i16): i16;

	fun rem(self, m: i16): i16 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		self
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl u16 {
	[extern("Math.pow")]
	external fun pow(self, exponent: u16): u16;

	[extern("Math.min")]
	external fun min(self, other: u16): u16;

	[extern("Math.max")]
	external fun max(self, other: u16): u16;

	fun rem(self, m: u16): u16 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		self
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl i32 {
	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		self
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl u32 {
	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		self
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl i53 {
	[extern("Math.abs")]
	external fun abs(self): i53;

	[extern("Math.pow")]
	external fun pow(self, exponent: i53): i53;

	[extern("Math.min")]
	external fun min(self, other: i53): i53;

	[extern("Math.max")]
	external fun max(self, other: i53): i53;

	fun rem(self, m: i53): i53 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		self
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl u53 {
	[extern("Math.pow")]
	external fun pow(self, exponent: u53): u53;

	[extern("Math.min")]
	external fun min(self, other: u53): u53;

	[extern("Math.max")]
	external fun max(self, other: u53): u53;

	fun rem(self, m: u53): u53 {
		self - self / m * m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		self
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl f32 {
	[extern("Math.abs")]
	external fun abs(self): f32;

	[extern("Math.sqrt")]
	external fun sqrt(self): f32;

	[extern("Math.pow")]
	external fun pow(self, exponent: f32): f32;

	[extern("Math.floor")]
	external fun floor(self): f32;

	[extern("Math.ceil")]
	external fun ceil(self): f32;

	[extern("Math.round")]
	external fun round(self): f32;

	[extern("Math.trunc")]
	external fun trunc(self): f32;

	[extern("Math.min")]
	external fun min(self, other: f32): f32;

	[extern("Math.max")]
	external fun max(self, other: f32): f32;

	/// `self` confined to `min..=max` — see `f64::clamp` for why the floats
	/// carry their own rather than inheriting `Ord`'s default.
	fun clamp(self, min: f32, max: f32): f32 {
		self.min(max).max(min)
	}

	/// Truncated remainder (sign follows the dividend), the method form of `%`.
	fun rem(self, m: f32): f32 {
		self % m
	}

	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		self
	}
}

impl f64 {
	fun as_i8(self): i8 {
		let widened = self;
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self;
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self;
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self;
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self;
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self;
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self;
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self;
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self;
		label_f32(widened)
	}

	fun as_f64(self): f64 {
		self
	}
}

impl BigInt {
	[extern("Number")]
	external fun as_f64(self): f64;

	fun as_i8(self): i8 {
		let widened = self.as_f64();
		label_i8(fold_signed(widened, 256f, 128f))
	}

	fun as_u8(self): u8 {
		let widened = self.as_f64();
		label_u8(fold_unsigned(widened, 256f))
	}

	fun as_i16(self): i16 {
		let widened = self.as_f64();
		label_i16(fold_signed(widened, 65536f, 32768f))
	}

	fun as_u16(self): u16 {
		let widened = self.as_f64();
		label_u16(fold_unsigned(widened, 65536f))
	}

	fun as_i32(self): i32 {
		let widened = self.as_f64();
		label_i32(fold_signed(widened, 4294967296f, 2147483648f))
	}

	fun as_u32(self): u32 {
		let widened = self.as_f64();
		label_u32(fold_unsigned(widened, 4294967296f))
	}

	fun as_i53(self): i53 {
		let widened = self.as_f64();
		label_i64(widened.trunc())
	}

	fun as_u53(self): u53 {
		let widened = self.as_f64();
		label_u64(widened.trunc())
	}

	fun as_f32(self): f32 {
		let widened = self.as_f64();
		label_f32(widened)
	}
}

impl i8 with Default {
	fun default(): i8 {
		0i8
	}
}

impl i8 with PartialEq {
	fun eq(self, b: i8): bool {
		self == b
	}
}

impl i8 with Add {
	fun add(self, b: i8): i8 {
		self + b
	}
}

impl i8 with Sub {
	fun sub(self, b: i8): i8 {
		self - b
	}
}

impl i8 with Mul {
	fun mul(self, b: i8): i8 {
		self * b
	}
}

impl i8 with Div {
	fun div(self, b: i8): i8 {
		self / b
	}
}

impl u8 with Default {
	fun default(): u8 {
		0u8
	}
}

impl u8 with PartialEq {
	fun eq(self, b: u8): bool {
		self == b
	}
}

impl u8 with Add {
	fun add(self, b: u8): u8 {
		self + b
	}
}

impl u8 with Sub {
	fun sub(self, b: u8): u8 {
		self - b
	}
}

impl u8 with Mul {
	fun mul(self, b: u8): u8 {
		self * b
	}
}

impl u8 with Div {
	fun div(self, b: u8): u8 {
		self / b
	}
}

impl i16 with Default {
	fun default(): i16 {
		0i16
	}
}

impl i16 with PartialEq {
	fun eq(self, b: i16): bool {
		self == b
	}
}

impl i16 with Add {
	fun add(self, b: i16): i16 {
		self + b
	}
}

impl i16 with Sub {
	fun sub(self, b: i16): i16 {
		self - b
	}
}

impl i16 with Mul {
	fun mul(self, b: i16): i16 {
		self * b
	}
}

impl i16 with Div {
	fun div(self, b: i16): i16 {
		self / b
	}
}

impl u16 with Default {
	fun default(): u16 {
		0u16
	}
}

impl u16 with PartialEq {
	fun eq(self, b: u16): bool {
		self == b
	}
}

impl u16 with Add {
	fun add(self, b: u16): u16 {
		self + b
	}
}

impl u16 with Sub {
	fun sub(self, b: u16): u16 {
		self - b
	}
}

impl u16 with Mul {
	fun mul(self, b: u16): u16 {
		self * b
	}
}

impl u16 with Div {
	fun div(self, b: u16): u16 {
		self / b
	}
}

impl i53 with Default {
	fun default(): i53 {
		0i53
	}
}

impl i53 with PartialEq {
	fun eq(self, b: i53): bool {
		self == b
	}
}

impl i53 with Add {
	fun add(self, b: i53): i53 {
		self + b
	}
}

impl i53 with Sub {
	fun sub(self, b: i53): i53 {
		self - b
	}
}

impl i53 with Mul {
	fun mul(self, b: i53): i53 {
		self * b
	}
}

impl i53 with Div {
	fun div(self, b: i53): i53 {
		self / b
	}
}

impl u53 with Default {
	fun default(): u53 {
		0u53
	}
}

impl u53 with PartialEq {
	fun eq(self, b: u53): bool {
		self == b
	}
}

impl u53 with Add {
	fun add(self, b: u53): u53 {
		self + b
	}
}

impl u53 with Sub {
	fun sub(self, b: u53): u53 {
		self - b
	}
}

impl u53 with Mul {
	fun mul(self, b: u53): u53 {
		self * b
	}
}

impl u53 with Div {
	fun div(self, b: u53): u53 {
		self / b
	}
}

impl f32 with Default {
	fun default(): f32 {
		0f32
	}
}

impl f32 with PartialEq {
	fun eq(self, b: f32): bool {
		self == b
	}
}

impl f32 with Add {
	fun add(self, b: f32): f32 {
		self + b
	}
}

impl f32 with Sub {
	fun sub(self, b: f32): f32 {
		self - b
	}
}

impl f32 with Mul {
	fun mul(self, b: f32): f32 {
		self * b
	}
}

impl f32 with Div {
	fun div(self, b: f32): f32 {
		self / b
	}
}
